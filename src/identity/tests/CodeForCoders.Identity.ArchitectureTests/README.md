# ArchitectureTests — template

The architecture suite was copied from the local .NET skill asset and adapted to
the `CodeForCoders.Identity` layer anchors.

Run it in `Debug`: ArchUnitNET reads IL and Release may optimise dependencies away.
