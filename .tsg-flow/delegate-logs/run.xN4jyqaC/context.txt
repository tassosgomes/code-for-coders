# Decisões operacionais do orquestrador

- Este PRD roda na worktree `/home/tsgomes/github-tassosgomes/code-for-coders-acesso-interno`
  (branch `feature/acesso-interno`). Trabalhe apenas nela; não altere o checkout principal
  `/home/tsgomes/github-tassosgomes/code-for-coders` nem a branch `main`.
- `tasks/prd-acesso-interno/flow-state.json` pertence ao orquestrador: não edite.
- Logs do transporte ficam fora da worktree; nada de `.tsg-flow/` entra em commits.
- Não suba a stack do Docker Compose: ela usa nomes de containers/volumes fixos compartilhados com o
  checkout principal. Testcontainers (testes) pode ser usado normalmente. Registre o smoke local
  como não executado no relatório.
- Lição das tasks anteriores: o gate usa filtros; antes de encerrar, rode também as suítes
  **completas** dos componentes tocados (Identity IntegrationTests, Notification IntegrationTests,
  BFF-admin EndToEndTests e `npm --prefix src/admin-spa run test`), que são passos obrigatórios do CI.
- Lição da 4.0: respeite os limites (`maxLength`, enums, códigos de erro) declarados nos contratos
  (`internal-api-contract.yaml`, `api-contract.yaml`, `asyncapi-contract.yaml`) em todas as camadas, e
  cubra-os com teste; o ato para a Auditoria deve validar contra `AtoPraticadoPayload`.
- **Não edite contratos aprovados** (`*.yaml`, `contracts.md`, `*-contract.md`) nem a techspec. Se um
  contrato tiver lacuna (código/status não definido), pare e devolva TASK BLOCKED com a lacuna, em vez
  de escolher um valor sozinho.

## Correção pós-full da task 3.0 (tentativa 3/3) — bloqueante B1

Revisão full: `tasks/prd-acesso-interno/prd_review.md` (run.QeuEMpDO). O job de CI `bff-admin` reprova
por cobertura 67,95% < 70%, e 377 linhas de `obj/.../OpenApiXmlCommentSupport.generated.cs` com 0% entram na
métrica.

**Decisão do usuário:** seguir o precedente já versionado em `src/audit/tests/testconfig.json` e
`src/bff-student/tests/testconfig.json` (Microsoft.Testing.Platform code coverage: `Sources.Exclude` de
`obj/` e `*.g.cs`/`*.generated.cs`). Nesta task:
1. Copie o mesmo `tests/testconfig.json` (conteúdo idêntico) para `src/bff-admin/tests/`,
   `src/identity/tests/` e `src/notification/tests/`. Confirme como os projetos de teste do audit/bff-student
   o carregam (ex.: item no csproj/Directory.Build.props de `tests/`) e replique o mecanismo exatamente.
2. Meça a cobertura do bff-admin como o CI mede: `dotnet test --no-restore --configuration Debug --coverage
   --coverage-output-format cobertura` em `src/bff-admin` e cobertura por **união de linhas** de todos os
   relatórios Cobertura (veja `prd_review.md` §1). Faça o mesmo para identity e notification.
3. Se o bff-admin continuar < 70%, adicione testes nas lacunas listadas em B1 (ramos de erro/timeout dos
   clientes, `BffSecurityMiddleware`, `ValkeyBffSessionStore` via IntegrationTests), sem enfraquecer testes.
- Não toque commerce, learning e media (ficam para a 8.0). Não altere código de produção salvo necessidade
  de testabilidade justificada.
- Registre no relatório a cobertura antes/depois de bff-admin, identity e notification, com o comando.
