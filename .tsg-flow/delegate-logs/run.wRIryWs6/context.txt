# Decisões operacionais do orquestrador

- Este PRD roda na worktree `/home/tsgomes/github-tassosgomes/code-for-coders-acesso-interno`
  (branch `feature/acesso-interno`). Trabalhe apenas nela; não altere o checkout principal
  `/home/tsgomes/github-tassosgomes/code-for-coders` nem a branch `main`.
- `tasks/prd-acesso-interno/flow-state.json` pertence ao orquestrador: não edite.
- Logs do transporte ficam fora da worktree; nada de `.tsg-flow/` entra em commits.
- Não suba a stack do Docker Compose: ela usa nomes de containers/volumes fixos compartilhados com o
  checkout principal. Testcontainers (testes) pode ser usado normalmente. Registre o smoke local
  como não executado no relatório.
- Lição das tasks anteriores: o gate usa filtros; antes de encerrar, rode também as suítes
  **completas** dos componentes tocados (Identity IntegrationTests, Notification IntegrationTests,
  BFF-admin EndToEndTests e `npm --prefix src/admin-spa run test`), que são passos obrigatórios do CI.
- Lição da 4.0: respeite os limites (`maxLength`, enums, códigos de erro) declarados nos contratos
  (`internal-api-contract.yaml`, `api-contract.yaml`, `asyncapi-contract.yaml`) em todas as camadas, e
  cubra-os com teste; o ato para a Auditoria deve validar contra `AtoPraticadoPayload`.
- **Não edite contratos aprovados** (`*.yaml`, `contracts.md`, `*-contract.md`) nem a techspec. Se um
  contrato tiver lacuna (código/status não definido), pare e devolva TASK BLOCKED com a lacuna, em vez
  de escolher um valor sozinho.

## Correção pós-full da task 8.0 (tentativa 2/3) — bloqueantes B2 e B3

Revisão full: `tasks/prd-acesso-interno/prd_review.md` (run.QeuEMpDO). Jobs de CI acionados reprovam por
cobertura < 70%: commerce 59,87% (B2), learning 55,69% e media 56,98% (B3; acionados porque o PRD alterou
`Directory.Packages.props`). O arquivo gerado `obj/.../OpenApiXmlCommentSupport.generated.cs` (377 linhas, 0%)
entra na métrica.

**Decisão do usuário:** seguir o precedente versionado (`src/bff-student/tests/Directory.Build.props` +
`tests/testconfig.json`, já replicado pela task 3.0 em bff-admin, identity e notification — veja o commit
`13f2d8b`). Nesta task:
1. Replique os mesmos dois arquivos, idênticos ao precedente, em `src/commerce/tests/`, `src/learning/tests/`
   e `src/media/tests/`. Se algum desses já tiver `tests/Directory.Build.props`, integre sem perder o conteúdo.
2. Meça como o CI (em `src/<svc>`: `dotnet restore && dotnet test --no-restore --configuration Debug --coverage
   --coverage-output-format cobertura`; união de linhas de todos os relatórios Cobertura recém-gerados, com o
   script do `tassosgomes/template-pipeline/.github/workflows/ci-dotnet.yml@v1`, disponível via `gh api`).
3. Onde ainda ficar < 70%, adicione testes significativos nas lacunas (commerce: `GlobalExceptionHandler`,
   `FinanceAreaJwksConfigurationManager` ramos de falha/renovação do JWKS, `OutboxPublisherWorker`, health
   checks — ver B2). Para learning/media, só adicione testes se necessário e mantendo-os significativos.
   Não enfraqueça testes nem altere código de produção salvo necessidade de testabilidade justificada.
- Registre cobertura antes/depois de commerce, learning e media com o comando usado.
