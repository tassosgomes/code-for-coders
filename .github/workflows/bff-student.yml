name: bff-student

on:
  push:
    branches: [main]
    paths:
      - 'src/bff-student/**'
      - 'Directory.*'
      - 'global.json'
      - 'BannedSymbols.txt'
      - '.github/workflows/bff-student.yml'
  pull_request:
    paths:
      - 'src/bff-student/**'
      - 'Directory.*'
      - 'global.json'
      - 'BannedSymbols.txt'
      - '.github/workflows/bff-student.yml'

permissions:
  contents: read
  security-events: write
  packages: write

jobs:
  ci:
    uses: tassosgomes/template-pipeline/.github/workflows/ci-dotnet.yml@v1
    with:
      working-directory: src/bff-student
      docker-context: .
      dockerfile: src/bff-student/Dockerfile
      version: '10.0.x'
      test-configuration: Debug
      coverage-threshold: 70
      build-container: true
      image-name: code-4-coders-bff-student
      security-mode: observe
