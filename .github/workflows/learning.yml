name: learning

on:
  push:
    branches: [main]
    paths:
      - 'src/learning/**'
      - 'Directory.*'
      - 'global.json'
      - 'BannedSymbols.txt'
      - '.github/workflows/learning.yml'
  pull_request:
    paths:
      - 'src/learning/**'
      - 'Directory.*'
      - 'global.json'
      - 'BannedSymbols.txt'
      - '.github/workflows/learning.yml'

permissions:
  contents: read
  security-events: write
  packages: write

jobs:
  ci:
    uses: tassosgomes/template-pipeline/.github/workflows/ci-dotnet.yml@v1
    with:
      working-directory: src/learning
      docker-context: .
      dockerfile: src/learning/Dockerfile
      version: '10.0.x'
      test-configuration: Debug
      coverage-threshold: 70
      build-container: true
      image-name: code-4-coders-learning
      security-mode: observe
