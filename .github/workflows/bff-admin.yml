name: bff-admin

on:
  push:
    branches: [main]
    paths:
      - 'src/bff-admin/**'
      - 'Directory.*'
      - 'global.json'
      - 'BannedSymbols.txt'
      - '.github/workflows/bff-admin.yml'
  pull_request:
    paths:
      - 'src/bff-admin/**'
      - 'Directory.*'
      - 'global.json'
      - 'BannedSymbols.txt'
      - '.github/workflows/bff-admin.yml'

permissions:
  contents: read
  security-events: write
  packages: write

jobs:
  ci:
    uses: tassosgomes/template-pipeline/.github/workflows/ci-dotnet.yml@v1
    with:
      working-directory: src/bff-admin
      docker-context: .
      dockerfile: src/bff-admin/Dockerfile
      version: '10.0.x'
      test-configuration: Debug
      coverage-threshold: 70
      build-container: true
      image-name: code-4-coders-bff-admin
      security-mode: observe
