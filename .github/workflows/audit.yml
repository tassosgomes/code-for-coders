name: audit

on:
  push:
    branches: [main]
    paths:
      - 'src/audit/**'
      - 'Directory.*'
      - 'global.json'
      - 'BannedSymbols.txt'
      - '.github/workflows/audit.yml'
  pull_request:
    paths:
      - 'src/audit/**'
      - 'Directory.*'
      - 'global.json'
      - 'BannedSymbols.txt'
      - '.github/workflows/audit.yml'

permissions:
  contents: read
  security-events: write
  packages: write

jobs:
  ci:
    uses: tassosgomes/template-pipeline/.github/workflows/ci-dotnet.yml@v1
    with:
      working-directory: src/audit
      docker-context: .
      dockerfile: src/audit/Dockerfile
      version: '10.0.x'
      test-configuration: Debug
      coverage-threshold: 70
      build-container: true
      image-name: code-4-coders-audit
      security-mode: observe
