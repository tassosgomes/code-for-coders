# Revisão da Task 1.0 — Registrar ato administrativo conforme

Run: run.TiT56OCG
Modo: focused · Tentativa: 1/3 · Data: 2026-09-24
HEAD (checkpoint): `ffa246fdc53acd1b392aab02a3c890f53ac66d01` — diff revisado = working tree não commitada (tracked + untracked do escopo `src/audit`)

## Resultado

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 4 recomendações.

## Gate e verificações do projeto

| Check | Comando | Exit | Resultado |
|---|---|---|---|
| Gate | `dotnet test --project src/audit/tests/CodeForCoders.Audit.IntegrationTests/CodeForCoders.Audit.IntegrationTests.csproj -- --filter-class CodeForCoders.Audit.IntegrationTests.AdministrativeActRecordingTests --minimum-expected-tests 6` | 0 | 7/7 passaram (≥ 6 exigidos; `gate_expect` atendido) |
| Lint | `dotnet format src/audit/CodeForCoders.Audit.slnx --verify-no-changes` | 0 | sem alterações |
| Build | `dotnet build src/audit/CodeForCoders.Audit.slnx` | 0 | 0 warnings, 0 errors |
| Arquitetura | `dotnet test --project src/audit/tests/CodeForCoders.Audit.ArchitectureTests/...csproj` | 0 | 8/8 |
| Unitários | `dotnet test --project src/audit/tests/CodeForCoders.Audit.UnitTests/...csproj` | 0 | 4/4 |
| Integração (suíte inteira do projeto) | `dotnet test --project src/audit/tests/CodeForCoders.Audit.IntegrationTests/...csproj` | 0 | 7/7 |
| Modelo EF x migrations | `dotnet ef migrations has-pending-model-changes` (Infra.Data / Api) | 0 | "No changes have been made to the model since the last migration." |

Todos os comandos executados via `rtk proxy`, em primeiro plano, com Testcontainers (PostgreSQL + RabbitMQ). Cobertura e `dotnet publish` ficam para a full, conforme a task.

## Rastreabilidade — "Pronto quando"

- [x] Gate exit 0 com ≥ 6 testes (7).
- [x] Quatro tipos geram registros conformes com todos os campos — `RecordsInternalInvitationIssued`, `RecordsInternalInvitationAcceptedWithoutReason`, `RecordsRoleGranted`, `RecordsRoleRevoked` via `AssertConformingRecord` (tenant, origem, fatoId, tipo, autor, alvo, complemento, motivo, `praticado_em` preservado, `recebido_em` = `FixedTimeProvider`, `conforming`, razões vazias, impressão de 64 hex).
- [x] `email`/`nome` fora do contrato não aparecem em colunas nem logs; motivo ausente de logs; spans com só `fatoId`, `origem`, `tenantId`, `tipo` — `DiscardsFieldsOutsideTheContractAndKeepsTelemetryPrivate`.
- [x] Base nova aplica todas as migrations: tabela reconstruída (14 colunas novas, 0 legadas), unicidade (`origem`, `fato_id`), índice (`tenant_id`, `praticado_em`), trigger `audit_records_append_only` ativo e função presente — `FreshMigrationRebuildsTableAndProtectsRecords`.
- [x] Tenants distintos — `RecordsEachActInItsOwnTenant`.

## Revisão semântica (pontos conferidos)

- **Ack após commit:** `AuditEventConsumerWorker.cs` chama `recorder.RecordAsync` (append + `CommitAsync`) antes de `BasicAckAsync`. OK.
- **Política única no Domain:** `Domain/Policies/AdministrativeActPolicy.cs` define os quatro tipos e a exigência de motivo; `AuditRecord.CreateConforming` a usa. OK (RN-A04/A05).
- **Descarte de campos:** `JsonUnmappedMemberHandling.Skip` no consumidor; DTO `AtoPraticado` só com campos do contrato; `AuditEventV1`, `AuditReceiptStore`, `IAuditEventRecorder` e o caso de uso legado removidos; nenhuma referência residual (`git grep`).
- **Relógio:** `TimeProvider` registrado como singleton e usado no caso de uso; teste substitui por relógio fixo.
- **Impressão digital:** SHA-256 da forma canônica de tenantId, tipo, praticadoEm (UTC), autor, alvo, complemento ordenado, motivo — alinhada a `techspec.md` V-02 (campos descartados não entram).
- **Migration:** nova migration `20260924165555_RebuildAdministrativeActRecords` gerada por EF (snapshot consistente, sem pending changes); SQL de trigger/função acrescentado ao `Up`/`Down`; `InitialAudit` não foi alterada.
- **Topologia:** fila `audit.acts` quorum com DLX e `x-delivery-limit`, binding `auditoria.ato-praticado.v1`; binding legado deixou de ser declarado.
- **Risco da TechSpec:** `EnableSensitiveDataLogging` removido em `Infra.Data/DependencyInjection.cs`.
- **Métrica:** `audit.acts.recorded` com tags `origin`, `type`, `conformity`.

## Bloqueantes

Nenhum.

## Recomendações (não bloqueiam)

1. **Exceção de domínio no consumidor** — `src/audit/src/CodeForCoders.Audit.Infra.Messaging/AuditEventConsumerWorker.cs:74-101`: `EntityValidationException` (ato não conforme) e falhas de banco não são capturadas; a entrega fica sem ack/nack. Está no escopo declarado de 3.0/4.0 — garantir que essas tasks cubram o caminho.
2. **Fila/binding legados em brokers existentes** — a topologia apenas deixa de declarar `audit.audit-event`/`audit.audit.event.v1`; ambientes já criados mantêm a fila antiga ligada ao exchange (acumulando mensagens se alguém publicar). Aceitável por o sistema estar fora de produção; considerar `QueueUnbind`/remoção ou nota operacional.
3. **Métrica sem teste** — nenhum teste verifica `audit.acts.recorded` e suas tags (`origin`, `type`, `conformity`). Não é critério do "Pronto quando", mas é saída observável da spec.
4. **Colunas anuláveis** — `autor_*`, `alvo_*`, `praticado_em` são `nullable` na tabela (preparando registros não conformes da 3.0). Coerente com a TechSpec; apenas registrar que a garantia de preenchimento dos conformes está só no Domain.

## Integridade da revisão

`git status --porcelain` e HEAD inalterados durante a revisão (nenhum arquivo de código editado pelo validator).
