# Revisão da task 3.0 — Registrar e alertar ato não conforme (V-03)

Run: run.Hexj6mzl
Modo: focused · Tentativa: 1/3 · Data: 2026-09-24
HEAD revisado: `8897dbffbf7f527fabab9ee1e49715b8a5a207e0` + árvore de trabalho não commitada (escopo da task)

## Resultado

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 4 recomendações.

## Gate e verificações do projeto

| Check | Comando | Exit | Resultado |
|---|---|---|---|
| Gate (unit) | `dotnet test --project …UnitTests.csproj -- --filter-class …AdministrativeActConformityTests --minimum-expected-tests 9` | 0 | 12/12 passaram |
| Gate (integração) | `dotnet test --project …IntegrationTests.csproj -- --filter-class …NonConformingActRecordingTests --minimum-expected-tests 4` | 0 | 4/4 passaram |
| Lint | `dotnet format src/audit/CodeForCoders.Audit.slnx --verify-no-changes` | 0 | sem mudanças |
| Build | `dotnet build src/audit/CodeForCoders.Audit.slnx` | 0 | 0 warnings, 0 erros |
| Arquitetura | `dotnet test --project …ArchitectureTests.csproj` | 0 | 8/8 |
| Unitários | `dotnet test --project …UnitTests.csproj` | 0 | 16/16 |
| Integração (regressão, extra) | `dotnet test --project …IntegrationTests.csproj` | 0 | 15/15 (V-01, V-02 e V-03) |
| Migração | `dotnet ef migrations has-pending-model-changes` | 0 | modelo alinhado ao snapshot |

`gate_expect` ("pelo menos 13 testes: 9 unitários + 4 integração"): atendido, com 12 + 4 = 16.
Todos os comandos rodaram via `rtk proxy`, em primeiro plano ou aguardados até o fim, com exit code registrado.

## Escopo revisado

Diff desde o checkpoint `8897dbf` mais os arquivos novos: `AtoPraticadoJsonConverter.cs`, a migração
`20260924174803_RecordNonConformingAdministrativeActs` (Designer e snapshot gerados pelo EF),
`AdministrativeActConformityTests.cs` e `NonConformingActRecordingTests.cs`. Também revisei as
mudanças em `AuditRecord`, `AdministrativeActPolicy`, `AdministrativeAct`/`Reference`,
`AuditRecordConfiguration`, `AuditTelemetry` e `RecordAdministrativeAct`.

## Conferência de critérios

- **Códigos de razão (tabela V-03):** `AdministrativeActPolicy.GetNonConformityReasons` gera os 7
  códigos estáveis numa política única no Domain. Um código só aparece quando a condição dele ocorre.
  Cada código tem teste unitário, e há um teste para a combinação em ordem.
  `convite-interno-aceito` sem motivo continua conforme.
- **Preservação:** o tipo desconhecido guarda o texto recebido; campos ausentes ficam nulos.
  `momento-ausente` grava `praticado_em` nulo. `complemento-invalido` não grava o complemento.
  `motivo-excede-limite` grava o motivo inteiro, com a coluna em `text`. O conversor tolerante
  transforma campos opcionais malformados em nulos ou razões, sem lançar exceção.
- **Alerta:** `audit.acts.nonconforming` tem só os atributos `origin` e `type`. O log de aviso traz
  `fatoId`, `origem`, `tipo` e as razões, sem motivo nem complemento. Um teste de integração confirma
  que o motivo não aparece no log.
- **Um alerta por ato / reentrega:** a telemetria só roda depois de um commit bem-sucedido. A
  reentrega cai em `AuditRecordAlreadyExistsException` e retorna antes. O teste de integração
  confirma 1 registro, 1 métrica e 1 aviso.
- **Sem descarte nem DLQ:** o worker dá ack nos atos legíveis não conformes; os testes checam o ack.
- **Migração:** gerada pelo tooling do EF, sem mudanças de modelo pendentes; `Down` é simétrico.
- **Fora do escopo respeitado:** a validação de `fatoId`/`origem`/`tenantId` continua no worker
  e em `ValidateReadableEnvelope` (4.0).

## Bloqueantes

Nenhum.

## Recomendações (não bloqueiam)

1. `RecordAdministrativeAct.cs:72-92`: ato não conforme agora incrementa só
   `audit.acts.nonconforming` e deixa de contar em `audit.acts.recorded`. Por isso, o atributo
   `conformity` desse contador fica sempre `conforming`. A spec não exige nenhuma das duas formas.
   Vale registrar a decisão para o painel e a QT-02, porque o total de atos gravados passa a ser a
   soma dos dois contadores.
2. `AuditRecord.cs` `ValidateReadableEnvelope`: `origem` com mais de 100 caracteres lança
   `EntityValidationException`. O worker só trata `JsonException`/`AlreadyClosedException`
   (`AuditEventConsumerWorker.cs:103-111`), então essa mensagem fica sem ack/nack. É o risco já
   mapeado na techspec para a V-04 (4.0), e a 4.0 deve cobrir esse caso.
3. `AtoPraticadoJsonConverter.cs`: o limite de 100 caracteres do complemento está repetido como
   literal, porque Contracts não referencia Domain. Uma constante local nomeada deixaria a
   duplicação explícita.
4. `3_task.md` cita `UseCases/Audit/RecordConsumedAuditEvent/*`, mas o caso de uso efetivo é
   `RecordAdministrativeAct`, que substituiu o antigo conforme a techspec. É só divergência de
   documentação.

## Estabilidade da revisão

O HEAD (`8897dbf`) e o `git status --porcelain` não mudaram durante a revisão. Não editei código,
status, tasks nem commits.
