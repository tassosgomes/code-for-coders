# Revisão focused — Task 5.0 (V-05) Demonstrar imutabilidade com a credencial de execução

Run: run.qTGaj4gk

Resultado atual (revalidação, rodada 3): **VALIDAÇÃO APROVADA** — ver seção "Revalidação — rodada 3" ao final. Rodada 2: run.uURFBNlF.

Run da revisão focused original: run.upr4TBZ9
Modo: focused · Tentativa: 1/3 · Data: 2026-09-24
HEAD revisado: `ae02a2bb54d6df592677e9e00879c61748ca880d` (checkpoint da 4.0) + working tree não commitada da 5.0
Hash do diff + untracked de `src/` no início e no fim da revisão: `01dd4a60…bd9e6d` (inalterado; HEAD inalterado)

## Resultado

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 4 recomendações.

## Gate (executado sem alteração, via `rtk proxy`)

| Comando | Exit | Saída |
|---|---|---|
| `dotnet test --project …IntegrationTests.csproj -- --filter-class …AuditImmutabilityTests --minimum-expected-tests 7` | 0 | 8 total, 8 succeeded |
| `dotnet test --project …UnitTests.csproj -- --filter-class …AuditAppendOnlyGuardrailTests --minimum-expected-tests 2` | 0 | 2 total, 2 succeeded |

`gate_expect` ("pelo menos 9 testes: 7 integração + 2 unitários") atendido: 10 testes passaram (8 + 2).

## Verificações do projeto

| Componente | Comando | Exit | Resultado |
|---|---|---|---|
| `src/audit` | `dotnet format src/audit/CodeForCoders.Audit.slnx --verify-no-changes` | 0 | sem mudanças |
| `src/audit` | `dotnet build src/audit/CodeForCoders.Audit.slnx` | 0 | 0 warnings, 0 errors |
| `src/audit` | `dotnet test` ArchitectureTests | 0 | 8/8 |
| `src/audit` | `dotnet test` UnitTests | 0 | 16/16 |
| `src/audit` | `dotnet test` IntegrationTests (suíte completa, extra) | 0 | 30/30 — V-01..V-04 continuam passando com a credencial de execução |

## Evidência adicional (contexto do orquestrador)

PostgreSQL 18 efêmero (containers `val5-pg`/`val5-pg2`, removidos ao final):

1. **Caminho do Compose local** — `scripts/init-local-databases.sql` com `ON_ERROR_STOP=1` e `app_password`:
   exit 0; reexecução exit 0 (idempotente). Em seguida `dotnet ef database update` com
   `ConnectionStrings__MigrationConnection` (dona): exit 0, as 4 migrations aplicadas.
   Com `code_for_coders_audit_runtime`: `SELECT` ok; `TRUNCATE`/`DELETE` → `permission denied`.
   `code_for_coders_media` → `User does not have CONNECT privilege`. ACL final:
   `code_for_coders_audit_writer=ar` (só SELECT/INSERT).
2. **Banco sem login runtime pré-provisionado** — migrations aplicadas com a dona:
   `SecureAuditRuntimePermissions` falha com `42704: role "code_for_coders_audit_runtime" does not exist`
   (exit 1); a migration não é registrada em `__ef_migrations_history` (rollback transacional), as
   três anteriores permanecem. Comportamento fail-closed, coerente com a TechSpec (tabela de impacto,
   linha "Credencial do `audit` em ambientes já provisionados": pré-requisito de implantação; QT-01
   da plataforma). Não bloqueia; ver R1.
3. **CI / outros serviços** — `.github/workflows/` não contém E2E nem job que use `docker-compose.yml`
   ou `scripts/init-local-databases.sql`; o `audit.yml` usa `ci-dotnet.yml` com Testcontainers
   (o fixture provisiona os papéis). As mudanças do Compose se restringem ao serviço `audit`; o init
   só acrescenta o login runtime e revoga `CONNECT` no banco `audit`, sem tocar os demais bancos.
   `scripts/apps.sh` não referencia credenciais nem migrations.

## Rastreabilidade — Pronto quando

| Critério | Evidência | Status |
|---|---|---|
| Gate exit 0 com ≥ 9 testes | 8 + 2 passaram | OK |
| 3 mutações com execução falham por privilégio (42501) e registro idêntico | `RuntimeCredentialCannot{Update,Delete,Truncate}AuditRecords` comparam `to_jsonb(record)` antes/depois | OK |
| 2 mutações com a dona falham pelo trigger (P0001, "append-only") | `DatabaseOwnerTriggerRejects{Update,Delete}` | OK |
| Serviço sobe e registra com a credencial de execução (fixture e Compose) | `RuntimeCredentialCanAppendAuditRecords` + suíte V-01..V-04 com `RuntimeConnectionString`; Compose validado via init + migration (item 1 acima) | OK |
| Reinício + republicação mantêm contagem e conteúdo | `RestartAndIdenticalRedeliveryPreserveRecordContent` (2 hosts, métrica `identical`, count=1, snapshot igual) | OK |
| Outros serviços sem `CONNECT` | `ProducerServiceRolesCannotConnectToTheAuditDatabase` + item 1 | OK (ver R2) |
| `DbContext` recusa `Modified`/`Deleted` | `AuditAppendOnlyGuardrailTests` (2) sobre `AuditRecord` | OK |
| Conexão de execução ≠ migration | `DependencyInjection` usa `DefaultConnection`; `AuditDbContextFactory` usa `MigrationConnection` (fallback dona) | OK |
| Migration gerada pela ferramenta EF | `20260924184735_SecureAuditRuntimePermissions` com Designer + snapshot, SQL de grants em `migrationBuilder.Sql` | OK |

## Bloqueantes

Nenhum.

## Recomendações (não bloqueantes)

- **R1 — Documentar a pré-condição da migration.**
  `Migrations/20260924184735_SecureAuditRuntimePermissions.cs:15-16` exige que
  `code_for_coders_audit_runtime` e `code_for_coders_audit_writer` já existam e que o banco se chame
  `code_for_coders_audit` (nome fixo). O `AuditAppendOnlyGuardrail.md` não registra essa pré-condição
  do step de migration. Sugestão: citá-la no guardrail/QT-01 e, opcionalmente, usar `current_database()`
  num bloco `DO` para não fixar o nome do banco.
- **R2 — Teste de `CONNECT` não discrimina a migration.**
  `AuditIntegrationFixture.cs:87-98` faz `REVOKE CONNECT … FROM PUBLIC` antes de migrar, então
  `ProducerServiceRolesCannotConnectToTheAuditDatabase` continuaria verde se a migration perdesse o
  `REVOKE CONNECT`. Sugestão: deixar no fixture só a criação dos papéis e provar o revoke pela migration.
- **R3 — Superusuário no Compose local.** No Compose, `code_for_coders_identity` é o `POSTGRES_USER`
  (superusuário) e mantém `CONNECT` em `code_for_coders_audit` (`has_database_privilege = t`). A causa
  é anterior a esta task e não depende de grants, mas vale registrar no checkpoint para quem validar
  a regra "outros serviços sem CONNECT" localmente.
- **R4 — `Down` incompleto.** O `Down` não restaura `PUBLIC` (`CONNECT`/schema) ao estado anterior.
  É aceitável para um endurecimento de segurança, mas vale deixar explícito num comentário.

## Fora do escopo

`tasks/prd-trilha-auditoria/implementation-notes.md` (nota do orquestrador), provisionamento na
plataforma (QT-01), cobertura agregada e `dotnet publish` (ficam para a validação full).

---

## Revalidação — rodada 2 (antes da full)

Run: run.uURFBNlF
Modo: revalidation · Tentativa: 1/3 · Data: 2026-09-24
HEAD: `47973065304ef23e913023f295f8e5b3dca1b89e` (reabertura) + working tree (fix run.8L2NkpFw)
`sha256(git diff HEAD -- src)`: `81390f79…abcb721`, igual no início e no fim; HEAD inalterado.

### Resultado

**VALIDAÇÃO APROVADA**: 0 bloqueantes. R1, R2 e R4 foram resolvidos; R3 continua como recomendação (anterior a esta task, sem mudança).

### Diff revisado

- `Migrations/20260924184735_SecureAuditRuntimePermissions.cs`: o `GRANT`/`REVOKE CONNECT` no `Up` e no `Down` agora usa um bloco `DO` com `format('%I', current_database())` e não fixa mais o nome do banco. O `Down` ganhou um comentário explicando por que o `CONNECT` de `PUBLIC` não é restaurado.
- `AuditAppendOnlyGuardrail.md`: documenta a pré-condição (QT-01 cria `code_for_coders_audit_runtime`, `code_for_coders_audit_writer` e a membership) e diz que a migration roda conectada ao banco alvo.
- `AuditIntegrationFixture.cs`: remove `REVOKE CONNECT … FROM PUBLIC`, os revokes por papel e o `GRANT CONNECT`. O fixture só cria os papéis; o controle de `CONNECT` vem apenas da migration.

### Gate (sem alteração, via `rtk proxy`)

| Comando | Exit | Saída |
|---|---|---|
| IntegrationTests `--filter-class …AuditImmutabilityTests --minimum-expected-tests 7` | 0 | 8/8 |
| UnitTests `--filter-class …AuditAppendOnlyGuardrailTests --minimum-expected-tests 2` | 0 | 2/2 |

`gate_expect` foi atendido: 10 testes passaram (≥ 9).

### Verificações do projeto

| Comando | Exit | Resultado |
|---|---|---|
| `dotnet format src/audit/CodeForCoders.Audit.slnx --verify-no-changes` | 0 | sem mudanças |
| `dotnet build src/audit/CodeForCoders.Audit.slnx` | 0 | 0 warnings, 0 errors |
| `dotnet test` ArchitectureTests | 0 | 8/8 |
| `dotnet test` UnitTests | 0 | 16/16 |
| `dotnet test` IntegrationTests (suíte completa, extra) | 0 | 34/34 |
| `dotnet ef migrations has-pending-model-changes --project src/audit/src/CodeForCoders.Audit.Infra.Data` | 0 | "No changes have been made to the model since the last migration." |

### Itens revalidados

| Item | Verificação | Status |
|---|---|---|
| R1: pré-condição + nome do banco | O guardrail documenta a pré-condição. A migration usa `current_database()` no `Up` e no `Down`. O fixture aplica a migration em banco limpo (suíte verde) | Resolvido |
| R2: teste discrimina o revoke da migration | **Mutação** em worktree temporário (`git worktree add --detach` + patch da working tree), removendo só o `EXECUTE format('REVOKE CONNECT ON DATABASE %I FROM PUBLIC', …)` do `Up`. Resultado: `ProducerServiceRolesCannotConnectToTheAuditDatabase` **falhou** ("Role code_for_coders_bff_admin can connect to the audit database."), 7/8, exit 2. Worktree removido; `git status --porcelain` e HEAD iguais à linha de base | Resolvido |
| R4: comentário no `Down` | Comentário presente | Resolvido |
| (b) migration só no SQL escrito à mão | `git diff HEAD --stat` em `Migrations/` altera só `…SecureAuditRuntimePermissions.cs`; Designer e snapshot intactos; `has-pending-model-changes` exit 0 | OK |
| (c) caminho local via init + migration | `scripts/init-local-databases.sql` não mudou nesta rodada (último commit 75816c4). Ele continua revogando `CONNECT` de `PUBLIC` e dos papéis dos produtores e concedendo a `code_for_coders_audit`/`_runtime`. A migration repete o revoke/grant de forma idempotente e usa o mesmo banco conectado. As duas camadas são coerentes. No init, o dono mantém `CONNECT` por ser owner e por grant explícito | OK |

### Regressões

Nenhuma. A remoção do `GRANT CONNECT` ao dono/runtime no fixture não afeta o acesso. O dono (`code_for_coders_audit`, usuário do container) é owner/superusuário do banco de teste, e a migration concede `CONNECT` ao runtime (a suíte V-01..V-05 roda com `RuntimeConnectionString` e passa).

### Recomendações remanescentes

- R3 (inalterada): superusuário `code_for_coders_identity` no Compose local mantém `CONNECT`. É anterior a esta task e não bloqueia.

## Revalidação — rodada 3 (após a full `run.Wuikipl6`)

Run: run.qTGaj4gk

Escopo: bloqueantes B-01 e B-02 de `prd_review.md`. Correção: run.3L9XnKW4. HEAD `2549c20a745b302fe4d95c914364bd849bba1c9d`
(reabertura). Diff revisado: `git diff HEAD` (`AuditApiFactory.cs`, `5_task.md`) mais os untracked
`src/audit/tests/Directory.Build.props` e `src/audit/tests/testconfig.json`.

### Resultado

**VALIDAÇÃO APROVADA**: 0 bloqueantes e 2 recomendações novas (R5 e R6). R3 continua registrada.

### Gate (sem alteração, via `rtk proxy`, na árvore real)

| Comando | Exit | Saída |
|---|---|---|
| IntegrationTests `--filter-class …AuditImmutabilityTests --minimum-expected-tests 7` | 0 | 8/8 |
| UnitTests `--filter-class …AuditAppendOnlyGuardrailTests --minimum-expected-tests 2` | 0 | 2/2 |

`gate_expect` foi atendido: 10 testes passaram (≥ 9).

### Reprodução do job `Build & Test` (`tassosgomes/template-pipeline/.github/workflows/ci-dotnet.yml@v1`, lido via `gh api`)

Executado em `src/audit`, numa cópia limpa da árvore de trabalho (rsync sem `bin/`, `obj/`, `TestResults/` e `publish/`),
para que relatórios Cobertura antigos não entrem na agregação. O runner é Microsoft.Testing.Platform, e
`test-configuration` é `Debug`.

| Passo | Comando | Exit | Resultado |
|---|---|---|---|
| Restore | `dotnet restore` | 0 | OK |
| Lint | `dotnet format --verify-no-changes --no-restore` | 0 | sem mudanças |
| Testes | `dotnet test --no-restore --configuration Debug --coverage --coverage-output-format cobertura` | 0 | **59/59** (Architecture, Unit, Integration e **EndToEnd** passaram) |
| Cobertura ≥ 70% | script Python do workflow, copiado literalmente (união de linhas dos 4 `.cobertura.xml` encontrados por `find … grep -l '<coverage'`) | — | **83,45%** (1498/1795) |
| Publish | `dotnet publish --configuration Release --no-restore --output ./publish` | 0 | OK |

Build e warnings: `TreatWarningsAsErrors=true` na raiz. Os builds de Debug (test) e Release (publish) concluíram sem erro.

### Itens revalidados

| Item | Verificação | Status |
|---|---|---|
| B-01: E2E quebrada pela migration | `AuditApiFactory` cria `code_for_coders_audit_writer` (NOLOGIN) e `code_for_coders_audit_runtime` (LOGIN, membro do writer) antes de `MigrateAsync`, igual à pré-condição P-02/QT-01. O host sobe com a credencial de execução (`NpgsqlConnectionStringBuilder` com usuário/senha do runtime). `LiveHealthEndpointConfirmsAuditProcessIsAlive` passa. O teste não foi enfraquecido nem removido | Resolvido |
| B-02: cobertura < 70% | A exclusão fica em `src/audit/tests/testconfig.json` (`CodeCoverage.Sources.Exclude`: `.*[\\/]obj[\\/].*` e `.*\.g(enerated)?\.cs$`). Limite e workflow não mudaram | Resolvido |
| B-02: exclusão restrita a gerado | `git ls-files src/audit` não tem arquivo versionado que case com `/obj/`, `.g.cs` ou `.generated.cs` (rg exit 1). No agregado, o total caiu de 2172 para 1795 linhas (−377), exatamente as 377 de `OpenApiXmlCommentSupport.generated.cs` apontadas na full. As linhas cobertas continuam 1498, e nenhuma linha restante casa com os padrões. Nada mantido pela equipe foi escondido. Os descobertos continuam visíveis (`GlobalExceptionHandler.cs` 57, migrations 52/43/14, health checks etc.) | OK |
| B-02: CI consome a config | `src/audit/tests/Directory.Build.props` importa o `Directory.Build.props` da raiz (`GetPathOfFileAbove`, então `TreatWarningsAsErrors`/BannedSymbols se mantêm) e, para `IsTestProject == true`, copia `testconfig.json` como `<AssemblyName>.testconfig.json`. Esse é o arquivo que o MTP lê ao lado do assembly. Confirmado: os 4 projetos de teste têm `bin/Debug/net10.0/*.testconfig.json`, e a cobertura com o mesmo comando do workflow reflete a exclusão. O workflow roda em `working-directory: src/audit` com restore/test sem argumentos extras, então segue o mesmo caminho | OK |

### Regressões

Nenhuma. Suíte completa verde (59/59). `git status --porcelain` e HEAD da árvore real ficaram iguais antes e depois da validação (a execução do CI foi feita numa cópia).

### Recomendações (não bloqueiam)

1. **R5 — senha duplicada na factory de E2E.** `AuditApiFactory.ProvisionRuntimeCredentialsAsync` repete o literal `'audit-runtime-test-password'` em vez de derivá-lo de `RuntimePassword`. Se um dos dois mudar sozinho, o E2E quebra por autenticação (sem falso positivo). Sugestão: interpolar a constante.
2. **R6 — `publish` da solução inteira.** O passo `dotnet publish` do workflow publica também os projetos de teste em `./publish`, que agora inclui os `*.testconfig.json`. Isso já acontecia com os assemblies de teste antes desta rodada e é inofensivo, mas vale considerar `IsPublishable=false` nos testes em outra task.
3. R3 (inalterada) continua registrada.

Jobs de outros workflows acionados por `Directory.*` e os passos de segurança (gitleaks/Trivy/Semgrep) ficam para a próxima full, como registrado em `prd_review.md`.
