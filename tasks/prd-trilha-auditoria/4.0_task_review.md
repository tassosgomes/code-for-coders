# Revisão focused — Task 4.0 (V-04)

Run: run.4SFR2uPp
Modo: focused · Tentativa: 1/3 · Data: 2026-09-24
HEAD revisado: 5f037d1d12269f1f00eb7b753c2c6c73c69a5894 (diff da árvore de trabalho sobre o checkpoint 3.0)

## Resultado

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 3 recomendações.

## Gate e verificações

| Check | Comando | Exit | Resultado |
|---|---|---|---|
| Gate | `dotnet test --project src/audit/tests/CodeForCoders.Audit.IntegrationTests/CodeForCoders.Audit.IntegrationTests.csproj -- --filter-class CodeForCoders.Audit.IntegrationTests.AuditDeadLetterTests --minimum-expected-tests 5` | 0 | 7/7 passaram (≥ 5 esperados; gate_expect atendido) |
| Lint | `dotnet format src/audit/CodeForCoders.Audit.slnx --verify-no-changes` | 0 | sem mudanças |
| Build | `dotnet build src/audit/CodeForCoders.Audit.slnx` | 0 | 0 warnings, 0 erros |
| Arquitetura | `dotnet test --project src/audit/tests/CodeForCoders.Audit.ArchitectureTests/...csproj` | 0 | 8/8 |
| Unitários | `dotnet test --project src/audit/tests/CodeForCoders.Audit.UnitTests/...csproj` | 0 | 16/16 |

Todos rodaram via `rtk proxy`, em primeiro plano, com Testcontainers reais (PostgreSQL e RabbitMQ).

## Escopo revisado

- `AuditEventConsumerWorker.cs` — classificação de ilegibilidade (`body`, `fatoId`, `origem`, `tenantId`) antes do processamento; `nack(requeue:false)` para ilegível; captura de toda exceção não fatal no processamento com requeue; ack separado.
- `AuditTelemetry.cs` — contador `audit.messages.illegible` com tag `reason`.
- `AuditDeadLetterTests.cs` (novo) — 7 cenários: JSON malformado, sem `fatoId`, sem `origem`, `origem` fora do padrão, `origem` > 100 caracteres, sem `tenantId`, banco parado com mensagem em voo → DLQ → religar → reprocessar duas vezes → 1 registro.

## Critérios de pronto

- [x] O gate passa com ≥ 5 testes (7).
- [x] Cada tipo de ilegibilidade chega à DLQ com o corpo idêntico (`Assert.Equal(body, deadLetterBody)`), sem nenhum registro novo e com o contador marcado pelo `reason` certo.
- [x] Com o banco fora, a mensagem vai à DLQ após o limite; reprocessada duas vezes, gera 1 registro e a métrica de reentrega idêntica.
- [x] Nenhum log contém o corpo: o log de ilegível registra só `Reason`; o teste confere a ausência do payload privado.
- [x] Achados do contexto (review 1.0 rec. 1; review 3.0 rec. 2): `EntityValidationException` e falhas de banco agora têm reject com requeue; `origem` > 100 ou fora de `^[a-z][a-z0-9-]*$` (asyncapi-contract.yaml:139) é classificada como ilegível com `reason=origem`.

## Bloqueantes

Nenhum.

## Recomendações (não bloqueiam)

1. `AuditEventConsumerWorker.cs` `TryNackAsync`: o requeue usa `BasicRejectAsync` no lugar do `nack(requeue:true)` da techspec (§ falha transitória). O comentário explica o motivo: no RabbitMQ 4.3, requeue por `basic.nack` não conta para `x-delivery-limit`. O efeito é o mesmo que a spec pede e o teste de banco fora comprova. Vale registrar o desvio na techspec/ADR para que a próxima leitura não o "corrija" de volta.
2. `TryDeserializeAct`: um `fatoId`/`tenantId` presente mas que não é uuid (por exemplo `"abc"`) lança `JsonException` e é contado como `reason=body`, não `fatoId`/`tenantId`. A mensagem ainda vai à DLQ intacta, então não há perda; só a granularidade do contador diverge da task. Nenhum teste cobre esse caso.
3. O `BasicAckAsync` só captura `AlreadyClosedException`. Outras falhas do canal no ack (como `OperationInterruptedException`) sobem para o consumidor. O broker reentrega ao fechar o canal, então não há perda, mas a garantia "nenhuma exceção deixa a mensagem sem ack/nack" fica implícita nesse ponto.

## Limitações

- O sensor de discriminação não se aplica ao modo focused.
- Cobertura agregada e `dotnet publish` ficam para a full, como a task define.

---

# Revalidação — Task 4.0, rodada 2 (antes da full)

Run: run.Ve5MoN1j
Modo: revalidation · Tentativa: 1/3 · Data: 2026-09-24
HEAD revisado: 171105aeaddc9e5ba9f7c1052d644abaf4b15d05 (reabertura) + árvore de trabalho (`git diff HEAD`)
Fix revisado: run.RONTTrOG (somente testes; nenhum código de produção alterado)

## Resultado

**VALIDAÇÃO APROVADA**: 0 bloqueantes, 0 recomendações novas. A rec. 2 da focused foi **retirada** porque o achado estava incorreto.

## Gate e verificações

| Check | Comando | Exit | Resultado |
|---|---|---|---|
| Gate | `dotnet test --project src/audit/tests/CodeForCoders.Audit.IntegrationTests/CodeForCoders.Audit.IntegrationTests.csproj -- --filter-class CodeForCoders.Audit.IntegrationTests.AuditDeadLetterTests --minimum-expected-tests 5` | 0 | 11/11 (≥ 5; gate_expect atendido) |
| Lint | `dotnet format src/audit/CodeForCoders.Audit.slnx --verify-no-changes` | 0 | sem mudanças |
| Build | `dotnet build src/audit/CodeForCoders.Audit.slnx` | 0 | 0 warnings, 0 erros |
| Arquitetura | `dotnet test --project src/audit/tests/CodeForCoders.Audit.ArchitectureTests/CodeForCoders.Audit.ArchitectureTests.csproj` | 0 | 8/8 |
| Unitários | `dotnet test --project src/audit/tests/CodeForCoders.Audit.UnitTests/CodeForCoders.Audit.UnitTests.csproj` | 0 | 16/16 |

Todos os comandos rodaram via `rtk proxy`, em primeiro plano, com Testcontainers reais (PostgreSQL e RabbitMQ).

## Rec. 2 da focused (A-01): o achado estava incorreto

- A focused afirmou que um `fatoId`/`tenantId` presente mas não uuid lançaria `JsonException` e seria contado como `reason=body`. **Isso é falso.**
- `AtoPraticadoJsonConverter.ReadGuid` (`src/audit/src/CodeForCoders.Audit.Contracts/AtoPraticadoJsonConverter.cs:85-89`) só aceita `JsonValueKind.String` com `Guid.TryParse` bem-sucedido. Qualquer outro valor vira `Guid.Empty`, sem exceção. Isso inclui string não uuid e tipo errado, como número.
- `TryDeserializeAct` (`AuditEventConsumerWorker.cs:174-190`) classifica então `Guid.Empty` como `fatoId`/`tenantId`, conforme a TechSpec § V-04.
- A lacuna real era apenas de cobertura. O fix a fecha com 4 testes: `SendsMessageWithNonUuidFactIdToTheDeadLetterQueueIntact`, `SendsMessageWithNonStringFactIdToTheDeadLetterQueueIntact`, `SendsMessageWithNonUuidTenantIdToTheDeadLetterQueueIntact` e `SendsMessageWithNonStringTenantIdToTheDeadLetterQueueIntact`.

## Discriminação dos testes novos

Executada em um worktree temporário, isolado da árvore real.

- **Mutação** (`AtoPraticadoJsonConverter.cs:89`): `ReadGuid` passa a lançar `JsonException` para valor presente e inválido, que é exatamente o comportamento que a rec. 2 supunha. Com isso, o classificador devolveria `reason=body`.
- **Resultado:** os 4 testes novos **falharam** (11 no total: 7 passaram, 4 falharam, exit 2). Os 7 testes existentes seguiram verdes, como esperado, porque a mutação não os afeta.
- **Limpeza:** o worktree foi removido. `git status --porcelain` e HEAD (`171105a`) ficaram idênticos à linha de base.

Os testes discriminam: eles falhariam se o classificador contasse o caso como `body`.

## Regressões no diff novo

- O helper `AssertDeadLetteredIntactAsync` ficou **mais estrito** e nada se enfraqueceu:
  - passou a exigir `x-death.reason == "rejected"`, o que prova o `nack(requeue:false)` imediato em vez do esgotamento do limite de entregas;
  - passou a exigir exatamente um evento de métrica, com o `reason` esperado (`Assert.Single(metrics.Events)`).
- O teste de falha de banco mudou apenas a forma de ler a DLQ (`TakeDeadLetterAsync` no lugar de `TakeDeadLetterBodyAsync`). As asserções continuam as mesmas.
- Nenhum código de produção mudou.
- `4_task.md` e `flow-state.json` trazem apenas as mudanças de estado do fluxo.

## Estado das recomendações anteriores

1. Rec. 1 (`BasicRejectAsync` no lugar de `nack`): segue registrada como desvio aceito D-01 em `implementation-notes.md`. Não houve mudança.
2. Rec. 2: **retirada**. O achado estava incorreto e a cobertura foi adicionada, conforme descrito acima.
3. Rec. 3 (ack que captura só `AlreadyClosedException`): não houve mudança e ela continua não bloqueante.
