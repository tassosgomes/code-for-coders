# Revisão da task 2.0 — Não duplicar ato reentregue e alertar divergência

Run: run.DdNFNIDn
Modo: focused · Tentativa: 1/3 · Data: 2026-09-24
HEAD revisado: 35c28e4f407c2b0a44e4ca324720a77933f1f90b (diff não commitado desde o checkpoint da task 1.0)

## Resultado

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 4 recomendações.

## Gate e verificações

| Check | Comando | Exit | Resultado |
|---|---|---|---|
| Gate | `dotnet test --project src/audit/tests/CodeForCoders.Audit.IntegrationTests/CodeForCoders.Audit.IntegrationTests.csproj -- --filter-class CodeForCoders.Audit.IntegrationTests.AdministrativeActRedeliveryTests --minimum-expected-tests 4` | 0 | 4/4 passaram (Testcontainers PostgreSQL + RabbitMQ reais) |
| Lint | `dotnet format src/audit/CodeForCoders.Audit.slnx --verify-no-changes` | 0 | sem mudanças |
| Build | `dotnet build src/audit/CodeForCoders.Audit.slnx` | 0 | 0 warnings, 0 erros |
| Arquitetura | `dotnet test --project src/audit/tests/CodeForCoders.Audit.ArchitectureTests/...csproj` | 0 | 8/8 |
| Unitários | `dotnet test --project src/audit/tests/CodeForCoders.Audit.UnitTests/...csproj` | 0 | 4/4 |

`gate_expect` (≥ 4 testes: reentrega idêntica, fatoId novo, divergente, corrida) atendido: exatamente os 4 cenários foram executados.
Comandos executados via `rtk proxy`, em primeiro plano.

## Escopo revisado

- `AuditUnitOfWork.CommitAsync`: traduz `DbUpdateException` com `PostgresException` `23505` **e** constraint `ux_audit_records_origem_fato_id` para `AuditRecordAlreadyExistsException`; outras violações continuam propagando.
- `RecordAdministrativeAct`: a unicidade no commit é a fonte de verdade; a impressão existente é lida **depois** da violação (techspec §87). `identical` → contador, sem log; `divergent` → contador e `LogWarning` com `Origem`, `FatoId` e as duas impressões, sem motivo/complemento. Se a leitura não achar o registro, relança.
- `AuditRecordWriter.ReadFingerprintAsync` (AsNoTracking, por `origem`+`fato_id`); porta ganhou só "ler impressão", conforme techspec §76. O papel da aplicação tem `SELECT, INSERT` (techspec §206), e os testes rodam com `WriterRole` configurado.
- `AuditTelemetry.ActsRedelivered` = `audit.acts.redelivered` com tag `outcome`.
- Worker cria escopo por mensagem (`AuditEventConsumerWorker.cs:91`), então a entidade `Added` que sobra no DbContext após a falha é descartada; o `ack` acontece porque o caso de uso retorna normalmente.
- Testes de integração: idêntica (1 registro, `identical`, sem warning, DLQ vazia); fatoId novo (2 registros com a mesma impressão, sem contador); divergente (snapshot `to_jsonb` byte a byte igual, 1 registro, warning único com origem, fatoId e 2 impressões hex distintas incluindo a original, sem motivo/complemento, DLQ vazia); corrida (2 hosts consumidores, `LOCK TABLE ... SHARE MODE` segura os dois INSERTs, confirmados via `pg_stat_activity`, e depois libera → 1 registro, `identical`, DLQ vazia). A corrida é exercitada de fato, não por sorte de agendamento.

## Critérios "Pronto quando"

- [x] Gate passa com ≥ 4 testes.
- [x] Mesma mensagem 2× → 1 registro e incremento `identical`.
- [x] Conteúdo alterado com a mesma chave → original inalterado, nenhum novo, `divergent`, 1 warning sem motivo.
- [x] Duas entregas simultâneas → 1 registro e DLQ vazia.

## Bloqueantes

Nenhum.

## Recomendações (não bloqueiam)

1. `src/audit/src/CodeForCoders.Audit.Infra.Messaging/AuditEventConsumerWorker.cs:95` — em reentrega o worker ainda registra `"Administrative act recorded"` (Information), porque `RecordAsync` descarta `WasRedelivered`. O log engana na operação; vale diferenciar ou suprimir quando for reentrega.
2. `AdministrativeActRedeliveryTests.cs` — `WaitForOutcomeCountAsync` aceita `>=`; a spec pede "um `redelivered{identical}`" e "`divergent` = 1". Depois da espera, confirmar a contagem exata (`Assert.Single`/`Equal(1, ...)`) pegaria um incremento duplicado.
3. `AuditRecordAlreadyExistsException` está em `Domain/Exceptions`, mas representa um conflito de persistência traduzido pela Infra; `Application` (ao lado de `IUnitOfWork`) seria um lugar mais natural. O teste de arquitetura passa; é questão de preferência.
4. `RecordAdministrativeActOutput` passou a ter `RecordId`/`ReceivedOn` anuláveis para acomodar a reentrega, o que enfraquece o contrato do caminho feliz. Um resultado discriminado (gravado × reentregue) deixaria isso explícito.

## Limitações

- Revisão focused: sem sensor de discriminação (exclusivo do modo full) e sem cobertura agregada/`dotnet publish`, que ficam para a full, conforme a task.
- A canonicalização da impressão digital foi entregue na task 1.0 e não mudou aqui; o teste "mesmo conteúdo, fatoId novo" confirma que o `fatoId` não entra na impressão.

HEAD e árvore de trabalho não mudaram durante a revisão.
