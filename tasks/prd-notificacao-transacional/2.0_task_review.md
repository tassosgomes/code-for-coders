# Revisão — Task 2.0: Recusar pedido de envio inválido

Run: run.uu6kRfH1

- **Modo:** revalidation
- **Tentativa:** 1/3
- **HEAD no momento da revisão:** `46bff4d` (chore(tsg-flow): reopen task 2.0), com alteração não
  commitada no working tree (diff avaliado abaixo). HEAD e `git status --porcelain` idênticos no
  início e no fim da revisão.
- **Revisão anterior:** `run.LluBZDtP` (revalidation) — **VALIDAÇÃO APROVADA**, B1 (lint da
  migration) corrigido.
- **Motivo da reabertura:** full validation `run.Ul8nMYpY` — **B2**: mutante M12 sobrevive.
  A condição `!validationResult.IsValid` em `AcceptNotificationSendRequest.cs:33` (fallback
  "forma inválida", correção da B4 da Task 1.0) não tinha nenhum teste que a discriminasse —
  desligando-a, a suíte completa não detectava.

## Escopo revisado nesta tentativa

Diff desde o checkpoint `7401e5b` (checkpoint task 2.0) restrito à Task 2.0: apenas
`src/notification/tests/CodeForCoders.Notification.IntegrationTests/RejectInvalidSendRequestTests.cs`
foi alterado por este ciclo (confirmado por `git diff --stat HEAD` — 1 arquivo, 10 inserções/1
remoção). Mudança:

- Novo teste `RequestWithInvalidRecipientIsRefusedWithSpecificReason`: publica um pedido com
  `destinatario="student.example.com"` (sem `@`), demais campos válidos (`finalidade`/`modelo`
  presentes, `dados.nome`/`dados.link` válidos), e afirma via `AssertRequestIsRefusedAsync`:
  Registro de Entrega "recusado" com motivo `NotificationRefusalReasons.InvalidFormat`
  ("forma inválida"), nenhuma chamada ao fake do provedor, nenhum outbox de
  `mensagem-entregue` gravado, `Link` nulo.
- `CreateRequest` ganhou parâmetro `destinatario` (default `"student@example.com"`), sem alterar
  o comportamento dos cenários existentes (todos continuam usando o email válido por default).

Arquivos fora deste escopo, presentes no working tree mas não pertencentes a este ciclo:
- `tasks/prd-notificacao-transacional/2_task.md` — apenas a nota de reabertura do orquestrador
  (não é código).
- `.agents/skills/tsg-flow-orchestrator/scripts/routing.default.json` — já sinalizado como fora de
  escopo desde a revisão focused original; permanece inalterado por este ciclo.
- `tasks/prd-notificacao-transacional/prd_review.md` (untracked) e `.tsg-flow/` (untracked) —
  artefatos do orquestrador/relatório full, não código da task.
- `PurgeDeliveryRecordPersonalDataTests.cs` e `SendRequestIdempotencyTests.cs` — comparação
  `git diff --stat 7401e5b 46bff4d` mostra que essas mudanças entraram nos commits
  `checkpoint task 7.0` / `checkpoint task 3.0`, não neste ciclo da Task 2.0.

## Verificação do bloqueante B2

O motivo produzido (`NotificationRefusalReasons.InvalidFormat` = `"forma inválida"`) bate
exatamente com o que `AcceptNotificationSendRequest.cs:32-33` produz para o fallback de
validação, e com o que `AcceptNotificationSendRequestInputValidator.cs:14-17` recusa
(`RuleFor(Destinatario).NotEmpty().EmailAddress().MaximumLength(320)` — `"student.example.com"`
falha em `EmailAddress()`). `Link` nulo é garantido incondicionalmente por
`DeliveryRecord.CreateRefused` (`DeliveryRecord.cs:126`, `Link = null` fixo), consistente com os
demais cenários de recusa já cobertos.

**Sensor de discriminação (evidência direcionada ao mutante M12):** como esta é uma revalidação
focada em um bloqueante específico (não full), apliquei apenas a mutação já descrita no relatório
anterior, para confirmar que o teste novo a mata:

1. Linha de base registrada: `git status --porcelain` com 5 entradas, HEAD `46bff4d` (ver acima).
2. Backup do arquivo único afetado (`AcceptNotificationSendRequest.cs`, hash md5
   `60842bdca26b3ce5f7240c4a6d68280e`) copiado para fora do repositório (sem `git stash`).
3. Mutação aplicada: `?? (!validationResult.IsValid ? ... : null)` → `?? (false ? ... : null)`
   (fallback desligado, equivalente à mutação M12 do relatório full).
4. `dotnet test ... --filter-class RejectInvalidSendRequestTests` (sem `--minimum-expected-tests`,
   para ver todas as falhas): **total 5, failed 1** — a única falha é
   `RequestWithInvalidRecipientIsRefusedWithSpecificReason`. As outras 4 continuam passando (não
   dependem do fallback). O mutante é morto exclusivamente pelo teste novo.
5. Arquivo restaurado do backup; `md5sum` pós-restauração idêntico ao pré-mutação; `git status
   --porcelain` ao final idêntico à linha de base (5 entradas, mesmos arquivos). HEAD inalterado.

## Gate

Comando declarado no frontmatter da task, executado sem alteração, no código restaurado (pós
verificação do sensor):

```
dotnet test src/notification/tests/CodeForCoders.Notification.IntegrationTests/CodeForCoders.Notification.IntegrationTests.csproj -- --filter-class "CodeForCoders.Notification.IntegrationTests.RejectInvalidSendRequestTests" --minimum-expected-tests 3
```

- **Exit code:** 0
- **Resultado:** `total: 5, failed: 0, succeeded: 5, skipped: 0`
- **gate_expect ("3 testes passam"):** atendido — 5/5 sucesso, acima da contagem mínima de 3. O 5º
  teste é o `RequestWithInvalidRecipientIsRefusedWithSpecificReason` desta correção; o 4º
  (`RequestWithInvalidLinkIsRefusedWithSpecificReason`) já vinha da Task 1.0/B4-B5, revisado
  anteriormente.

## Regressão

Executada a suíte completa de integração do projeto de notificação
(`CodeForCoders.Notification.IntegrationTests`) para checar efeito colateral do diff:
**17/17 sucesso, 0 falhas**, exit 0. Não reproduziu a flakiness de contenção de recurso observada
na revisão anterior (B1); nenhuma regressão introduzida por este ciclo.

## Veredito

**VALIDAÇÃO APROVADA** — B2 (mutante M12) corrigido: novo teste discrimina o fallback de "forma
inválida", confirmado por reprodução da mutação original (mata o mutante, sem afetar os demais
cenários). Gate passa (5/5, exit 0). Suíte completa do projeto passa (17/17). Sem bloqueantes
novos. Sem recomendações adicionais além das já registradas nas revisões anteriores
(`run.zcTAElRa`, `run.LluBZDtP`), que continuam válidas.
