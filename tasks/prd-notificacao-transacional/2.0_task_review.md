# Revisão — Task 2.0: Recusar pedido de envio inválido

Run: run.LluBZDtP

- **Modo:** revalidation
- **Tentativa:** 1/3
- **HEAD no momento da revisão:** `0cdc96e` (chore(tsg-flow): reopen task 2.0), com alteração não
  commitada no working tree (diff avaliado abaixo). HEAD e `git status --porcelain` idênticos no
  início e no fim da revisão.
- **Revisão anterior:** `run.zcTAElRa` (focused) — **VALIDAÇÃO APROVADA**, sem bloqueantes, 2
  recomendações não bloqueantes.
- **Motivo da reabertura:** full validation `run.kgdLCoEc` — **B1 (parcial)**: `dotnet format`
  falhava em `20260921194427_AddDeliveryRecordRefusal.cs` (BOM UTF-8 contra `charset=utf-8`, recuo
  extra após conversão para namespace de arquivo). Task 2_task.md registra a reabertura como
  "sem alteração de comportamento — puramente formatação".

## Escopo revisado nesta tentativa

Diff desde o checkpoint `20a0ce3` (checkpoint task 2.0) restrito ao arquivo em
`Modificar/Referenciar` da reabertura: apenas
`src/notification/src/CodeForCoders.Notification.Infra.Data/Migrations/20260921194427_AddDeliveryRecordRefusal.cs`
foi alterado por este ciclo. O diff é puramente de formatação: remove o BOM UTF-8 da primeira linha
(`﻿using System;` → `using System;`) e reindenta o corpo da classe (remove um nível de indentação
herdado do estilo pré-file-scoped-namespace). Nenhuma linha de código funcional foi adicionada,
removida ou reordenada — comparação campo a campo confirma que `Up`/`Down` mantêm exatamente as
mesmas chamadas, tipos, nomes de coluna e valores default.

Arquivos fora deste escopo, presentes no working tree mas não pertencentes a este ciclo de
reabertura:
- `tasks/prd-notificacao-transacional/2_task.md` — apenas a seção "Reabertura pós-full" adicionada
  pelo orquestrador; não é código.
- `.agents/skills/tsg-flow-orchestrator/scripts/routing.default.json` — já sinalizado como fora de
  escopo na revisão focused anterior (`run.zcTAElRa`); permanece inalterado desde então e não afeta
  a Task 2.0.

Não relevante a este arquivo: uma segunda alteração de escopo apareceu em
`RejectInvalidSendRequestTests.cs` (novo teste `RequestWithInvalidLinkIsRefusedWithSpecificReason`)
desde o checkpoint original de 2.0 — mas essa mudança foi introduzida e já revisada como parte da
reabertura da **Task 1.0** (commit `298f99b`, correção de B4/B5; ver `1.0_task_review.md`, seção
B4/B5), não deste ciclo. Confirmado por `git show --stat` nos commits da reabertura: nenhum deles
atribuível à Task 2.0 toca esse arquivo de teste.

## Gate

Comando declarado no frontmatter da task, executado sem alteração:

```
dotnet test src/notification/tests/CodeForCoders.Notification.IntegrationTests/CodeForCoders.Notification.IntegrationTests.csproj -- --filter-class "CodeForCoders.Notification.IntegrationTests.RejectInvalidSendRequestTests" --minimum-expected-tests 3
```

- **Exit code:** 0
- **Resultado:** `total: 4, failed: 0, succeeded: 4, skipped: 0`
- **gate_expect ("3 testes passam"):** atendido — 4/4 sucesso, acima da contagem mínima de 3. O 4º
  teste (`RequestWithInvalidLinkIsRefusedWithSpecificReason`) pertence à correção de B4/B5 da Task
  1.0, não a este ciclo; sua presença é compatível com `--minimum-expected-tests 3` e não é uma
  regressão desta task.

## Verificação do bloqueante B1 (parcial)

- **BOM removido:** `head -c 3` do arquivo confirma ausência de BOM (`75 73 69` = `usi`, ASCII puro).
- **`dotnet format --verify-no-changes` no arquivo:** exit 0 — sem divergências de formatação.
- **Nenhuma mudança de comportamento:** confirmado por inspeção linha a linha do diff — apenas
  remoção de BOM e reindentação; `Up`/`Down`, nomes de coluna, tipos e valores default idênticos ao
  conteúdo já aprovado na revisão focused anterior.

## Regressão

Fora do gate declarado, executada a suíte completa de integração do projeto de notificação
(`CodeForCoders.Notification.IntegrationTests`, 16 testes) para checar efeito colateral:
- Primeira execução: 14/16 sucesso, 2 falhas (`AccountConfirmationRequestIsAcceptedDeliveredAndPublished`,
  `PasswordRecoveryRequestIsDeliveredWithItsOwnValidity`), ambas com mensagens de retry de outbox
  no log, indicando contenção de recurso ao rodar todos os testcontainers em paralelo.
- Reexecução isolada dessas duas classes: 2/2 sucesso — confirma flakiness ambiental, não
  regressão introduzida pela formatação da migration. Nenhuma delas cobre a Task 2.0.

## Veredito

**VALIDAÇÃO APROVADA** — B1 (parcial, migration da Task 2.0) corrigido sem alteração de
comportamento. Gate passa (4/4, exit 0). Sem bloqueantes novos. Sem recomendações adicionais além
das já registradas na revisão focused (`run.zcTAElRa`), que continuam válidas.
