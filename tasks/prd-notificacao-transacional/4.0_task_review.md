# Revalidação — Task 4.0: Entregar recuperação de senha com o mesmo mecanismo

Run: run.iyh7Nbmu

- **Modo:** revalidation; **tentativa:** 2/3.
- **Escopo:** bloqueio da revisão `run.7hWjlaTo`, diff funcional atual e regressões relacionadas.

## Gate

Comando declarado no frontmatter, executado sem alteração via `rtk proxy`:

```text
dotnet test src/notification/tests/CodeForCoders.Notification.IntegrationTests/CodeForCoders.Notification.IntegrationTests.csproj -- --filter-class "CodeForCoders.Notification.IntegrationTests.DeliverPasswordRecoveryEmailTests" --minimum-expected-tests 1
```

Exit code 0; `total: 1, succeeded: 1, failed: 0`. `gate_expect` satisfeito.

## Revalidação

- O bloqueio anterior de escopo/autorização foi resolvido: `flow-state.json` registra decisão humana, e `4_task.md:48-59` autoriza explicitamente generalizar RF-01 em `NotificationSendRequestRules.cs`. A regra aceita somente finalidade/modelo conhecidos e correspondentes (`NotificationSendRequestRules.cs:14-25`); `RejectInvalidSendRequestTests` passou 3/3.
- `MessageTemplateRenderer.cs:15-68` seleciona o modelo de recuperação e lê a validade própria; o cenário V-04 validou entrega, validade `2 hours`, ausência de descadastro, registro, evento e outbox. `DeliverPasswordRecoveryEmailTests` passou 1/1; confirmação de conta passou 1/1.
- A suíte completa passou 5/8; as três falhas (`HeartbeatFlowsThroughOutboxRabbitMqAndConsumer`, recuperação e confirmação) reproduzem a flakiness de outbox compartilhado já documentada na baseline. A decisão registrada em `4_task.md:70-77` manda não bloquear esta task; fica como recomendação técnica.
- HEAD permaneceu `a0fdebd696931cdfc343004432c6bffcfa61ec0c`; `git diff --check` passou e não houve alteração de código durante a revisão.

## Checklist e veredito

- [x] Gate focalizado e expectativa de teste.
- [x] Comportamento V-04 e regressões diretamente afetadas.
- [x] Bloqueio anterior resolvido com autorização registrada.

**VALIDAÇÃO APROVADA** — 0 bloqueantes; 1 recomendação não bloqueante: corrigir o isolamento da
suíte de integração/outbox antes de exigir estabilidade da suíte completa.
