# Revisão — Task 7.0 Expurgar dado pessoal do Registro de Entrega após a retenção

Run: run.av1vyQyC

**Modo:** revalidation · **Tentativa:** 1/3 · **Resultado: VALIDAÇÃO APROVADA**

## Gate da task

`dotnet test src/notification/tests/CodeForCoders.Notification.IntegrationTests/CodeForCoders.Notification.IntegrationTests.csproj -- --filter-class "CodeForCoders.Notification.IntegrationTests.PurgeDeliveryRecordPersonalDataTests" --minimum-expected-tests 1`

Exit 0; `total: 3, succeeded: 3, failed: 0` — `gate_expect` ("1 teste passa") atendido. **Gate: passed.**

## Revalidação do bloqueio da reabertura pós-full #2 (prd_review.md, run.Ul8nMYpY)

- **B1 (parcial — vazamento de estado entre testes via NO_ROUTE).** Confirmado no diff de
  `PurgeDeliveryRecordPersonalDataTests.cs`: os dois testes que geram uma entrega/falha antes do
  expurgo (`PurgeRemovesDeliveredRecordPersonalDataAndPreservesOutcomeCounter` e
  `PurgeRemovesFailedRecordPersonalDataAndPreservesOutcomeCounter`) agora sobem
  `RabbitMqConnectionProvider`/`RabbitMqPublisher`/`OutboxPublisherWorker` no host de teste, declaram
  uma fila exclusiva por execução (`CreateOutputQueueName`, nome com GUID v7) e a vinculam à routing
  key correspondente (`notificacao.mensagem-entregue.v1` e `notificacao.entrega-falhou.v1`) antes de
  disparar a transição. Cada teste consome e confirma (`AcknowledgePublishedMessageAsync` →
  `BasicGetAsync`/`BasicAckAsync`, com poll de até 15s) o evento publicado antes de rodar o expurgo.
  Isso fecha a lacuna do `NO_ROUTE`: a linha de outbox não fica mais pendente sem fila vinculada, e o
  `OutboxPublisherWorker` não trava atrás dela para o próximo teste da suíte.
- Nenhuma asserção de negócio pré-existente foi alterada — a mudança é só na preparação/consumo do
  evento de outbox; as asserções sobre `destinatario`/`nome`/`link`/`motivo` nulos e o contador
  agregado permanecem como antes (aprovadas na revalidation anterior, run.uU80mDiF).
- Padrão consistente com a correção já aplicada e aprovada na Task 3.0 na mesma reabertura
  (`SendRequestIdempotencyTests`, commits `bcd45ad`/`704cee4`).

## Evidências de regressão e persistência

- **Suíte completa do projeto de integração, sem filtro** (evidência pedida explicitamente pela
  reabertura #2, junto com a Task 3.0): exit 0, `total: 16, succeeded: 16, failed: 0`. Nenhum teste
  órfão/pendente por `NO_ROUTE` nem trava de `OutboxPublisherWorker` entre testes.
- Task 1.0 (`AcceptAndDeliverAccountConfirmationTests`): exit 0, `1/1`.
- Task 3.0 (`SendRequestIdempotencyTests`): exit 0, `2/2`.
- Task 4.0 (`DeliverPasswordRecoveryEmailTests`): exit 0, `1/1`.
- Task 5.0 (`RetryProviderTransientFailureTests`, `--minimum-expected-tests 2`): exit 0, `3/3`.
- Task 6.0 (`FailImmediatelyOnPermanentProviderErrorTests`): exit 0, `1/1`.
- `dotnet format src/notification --verify-no-changes --no-restore --include
  .../PurgeDeliveryRecordPersonalDataTests.cs`: exit 0 — sem violações de formatação no arquivo
  alterado.
- `git diff --check` sobre `src/notification/` e `7_task.md`: limpo.
- HEAD permaneceu `82ce149bac04af69a29a81310d22072738179bd8` durante toda a revisão; `git status
  --porcelain` inalterado (mesmas 3 modificações de escopo do worktree + `.tsg-flow/`/`prd_review.md`
  não rastreados, operacionais, fora do escopo desta task).
- Fora de escopo: `.agents/skills/tsg-flow-orchestrator/scripts/routing.default.json` aparece
  modificado no worktree, mas já constava alterado antes desta chamada de validação e não pertence ao
  diff desta task; não avaliado aqui.

Nenhum dos cinco gates de dependência (1.0, 3.0, 4.0, 5.0, 6.0) regrediu; nenhuma alteração de
comportamento observável foi introduzida fora do item B1 da reabertura #2. Sensor de discriminação
formal (mutação isolada em worktree) não se aplica ao modo `revalidation` — o bloqueio revalidado era
de infraestrutura de teste (vazamento de estado via fila não vinculada), não de cobertura de mutação.

## Veredito

Bloqueantes: 0. Recomendações: 0. **VALIDAÇÃO APROVADA** — gate `passed`, B1 (reabertura #2) corrigido
sem regressão nas Tasks 1.0, 3.0, 4.0, 5.0 e 6.0.
