# Revisão — Task 7.0 Expurgar dado pessoal do Registro de Entrega após a retenção

Run: run.uU80mDiF

**Modo:** revalidation · **Tentativa:** 1/3 · **Resultado: VALIDAÇÃO APROVADA**

## Gate da task

`dotnet test src/notification/tests/CodeForCoders.Notification.IntegrationTests/CodeForCoders.Notification.IntegrationTests.csproj -- --filter-class "CodeForCoders.Notification.IntegrationTests.PurgeDeliveryRecordPersonalDataTests" --minimum-expected-tests 1`

Exit 0; `total: 3, succeeded: 3, failed: 0` — `gate_expect` ("1 teste passa") atendido, agora com
2 testes novos cobrindo os desfechos entregue/falhou. **Gate: passed.**

## Revalidação dos bloqueios da reabertura pós-full (prd_review.md, run.kgdLCoEc)

- **B1 (lint na migration).** `dotnet format src/notification --verify-no-changes --no-restore
  --include .../Migrations/20260921225912_AddDeliveryOutcomeCounters.cs`: exit 0. BOM UTF-8 e
  recuo removidos; arquivo conforme `charset=utf-8`. Corrigido.
- **B5 (parcial — consistência do `Link`).** `DeliveryRecord.cs`: `CreateRefused` (:126),
  `MarkDelivered` (:147) e `MarkFailed` (:180) zeram `Link` na própria transição, como já corrigido
  na reabertura da Task 1.0. `PurgePersonalData` (:192) ainda atribui `Link = null` de novo — é um
  no-op sobre campo já nulo, não reexpurga nem lê valor antigo. Nenhum teste desta task depende do
  `Link` sobrevivendo até o expurgo (os dois testes novos afirmam `Link` nulo logo após a transição,
  antes do expurgo, e nulo depois). Consistente — nada a corrigir.
- **B7 (mutante M10 — expurgo ignorava "entregue").** `DeliveryRecordPurgeWorker.cs:64-67` mantém os
  três ramos (`Refused`, `Delivered`, `Failed`). `PurgeDeliveryRecordPersonalDataTests` ganhou
  `PurgeRemovesDeliveredRecordPersonalDataAndPreservesOutcomeCounter` e
  `PurgeRemovesFailedRecordPersonalDataAndPreservesOutcomeCounter`, cada um cria o registro via os
  casos de uso reais (`IAcceptNotificationSendRequest` + `IDeliverAcceptedNotification`, com sender
  de teste que entrega ou falha permanentemente), move a transição para antes do corte e confirma que
  `destinatario`/`nome`/`link`/`motivo` ficam nulos e `finalidade`/situação/timestamp permanecem.
  Removendo manualmente o ramo `Delivered` do worker (inspeção do código, não reexecutado como
  mutação isolada nesta revalidation) o novo teste de entrega passa a falhar — cobertura fecha a
  lacuna do mutante M10.
- **B8 (mutante M11 — contador não lido após entrega/falha).** Os mesmos dois testes novos leem
  `delivery_outcome_counters` antes e depois do expurgo para `Delivered` e `Failed` e afirmam
  `Assert.Equal(1, counterBeforePurge.Count)` e igualdade antes/depois. Isso força a suíte a
  depender do incremento em `DeliverAcceptedNotification.cs:70-73` (entrega) e `:114-117` (falha) —
  removê-los quebraria os novos testes, fechando a lacuna do mutante M11.

## Evidências de regressão e persistência

- Task 1.0 (`AcceptAndDeliverAccountConfirmationTests`, `--minimum-expected-tests 1`): exit 0,
  `1/1`.
- Task 4.0 (`DeliverPasswordRecoveryEmailTests`, `--minimum-expected-tests 1`): exit 0, `1/1`.
- Task 5.0 (`RetryProviderTransientFailureTests`, `--minimum-expected-tests 2`): exit 0, `3/3`
  (≥ 2 esperado; terceiro teste é de reabertura da própria Task 5.0, fora do escopo desta revisão).
- Task 6.0 (`FailImmediatelyOnPermanentProviderErrorTests`, `--minimum-expected-tests 1`): exit 0,
  `1/1`.
- `dotnet ef migrations has-pending-model-changes` (Infra.Data → Api,
  `NotificationDbContext`): exit 0, "No changes have been made to the model since the last
  migration." — migration e modelo sincronizados.
- `git diff --check` sobre `src/notification/` e `7_task.md`: limpo.
- HEAD permaneceu `51a5cc113291263871160b704f5c396653d32c59` durante toda a revisão; `git status
  --porcelain` inalterado (mesmas 4 modificações + `.tsg-flow/`/`prd_review.md` não rastreados,
  operacionais, fora do escopo desta task).

Nenhum dos quatro gates de dependência regrediu; nenhuma alteração de comportamento observável foi
introduzida fora do escopo dos itens B1/B5/B7/B8. Sensor de discriminação formal (mutação isolada em
worktree) não se aplica ao modo `revalidation` — a lacuna dos mutantes M10/M11 foi fechada por
inspeção da nova cobertura de teste, como descrito acima.

## Veredito

Bloqueantes: 0. Recomendações: 0. **VALIDAÇÃO APROVADA** — gate `passed`, B1/B5/B7/B8 corrigidos
sem regressão nas Tasks 1.0, 4.0, 5.0 e 6.0.
