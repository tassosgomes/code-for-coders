# Revisão — Task 5.0 (revalidation, reabertura pós-full)

Run: run.x1Z2rQJj

- Tentativa: 1/3; branch `feature/notificacao-transacional`; HEAD `48f2d5b3a4dcfe6579b2fc6ddb140a191b1aceef` (working tree com alterações não commitadas do implementer, estável durante toda a revisão).
- Escopo: bloqueios B1, B2, B3, B6 atribuídos a esta task na revisão full (`prd_review.md`, run.kgdLCoEc), diff novo desde a reabertura, e regressão nos gates das Tasks 1.0/6.0 que dependem deste worker.

## Gate

- Gate declarado: `dotnet test ...IntegrationTests.csproj -- --filter-class "...RetryProviderTransientFailureTests" --minimum-expected-tests 2`.
- Exit code 0; `total: 3`, `succeeded: 3`, `failed: 0`. `gate_expect` ("2 testes passam") satisfeito. Gate: **passed**.

## Revalidação dos bloqueantes

- **B1 (lint) — corrigido.** BOM UTF-8 removido de `20260921213348_AddTransactionalEmailRetryState.cs`. `dotnet format src/notification --verify-no-changes --no-restore` não reporta mais nenhum erro nesse arquivo (0 ocorrências). Os erros remanescentes do comando (`20260921194427_AddDeliveryRecordRefusal.cs`, `20260921225912_AddDeliveryOutcomeCounters.cs`) pertencem a migrations de outras tasks (2.0/3.0), fora do escopo desta reabertura — comando ainda sai 2 no agregado, mas isso não é regressão desta task.
- **B2 (arquitetura) — corrigido.** `TransactionalEmailDeliveryWorker` não chama mais `IDeliverAcceptedNotification` diretamente; agora usa `ITransactionalEmailDeliveryMessageHandler` (novo, em `Application.Interfaces`), implementado por `TransactionalEmailDeliveryMessageHandler` na camada `Api` — mesmo padrão já aplicado em `NotificationSendRequestConsumerWorker` pela Task 1.0. `dotnet test .../ArchitectureTests -- --filter-class LayerDependencyTest`: exit 0, `total: 4`, `succeeded: 4` (inclui `InfraDoesNotUseApiNorUseCases`).
- **B3 (crítico, timeout derruba o host) — corrigido.** `HttpTransactionalEmailSender` agora classifica `Polly.Timeout.TimeoutRejectedException` como `ProviderTimeout` (transitório) e `Polly.ExecutionRejectedException` (base de `TimeoutRejectedException`, confirmado por inspeção: `TimeoutRejectedException : ExecutionRejectedException : Exception`) como `ProviderUnavailable`, com a ordem de `catch` correta (subtipo antes do tipo base). `TransactionalEmailDeliveryWorker.ExecuteAsync` passou a capturar `DbUpdateException` e `ExecutionRejectedException` por ciclo, sem derrubar o `BackgroundService`. Cobertura nova: `HttpTransactionalEmailSenderTests.SendAsync_PollyTimeoutRejectedException_ThrowsTransientProviderTimeout` e `..._PollyExecutionRejectedException_ThrowsTransientProviderUnavailable`, exercitando os tipos reais do Polly (atende o "no mínimo" pedido pela reabertura).
- **B6 (mutante sobrevivente, backoff constante) — corrigido.** Novo teste `RetryPolicyUsesIncreasingExponentialBackoff` chama `TransactionalEmailRetryPolicy.GetBackoff` diretamente (isolado de polling) e afirma `GetBackoff(1)=40ms`, `GetBackoff(2)=80ms`, `GetBackoff(3)=160ms` — determinístico, mata a mutação "backoff constante" sem depender de limiares absolutos de tempo. O cálculo em si (`TransactionalEmailRetryPolicy.cs:13-23`) já usa `Math.Pow(multiplier, attemptNumber-1)` corretamente; o problema era só a discriminação do teste.

## Regressões

- Unit tests completos: `total: 15`, `succeeded: 15`.
- Integration tests completos: `total: 14`, `succeeded: 14`.
- Gate da Task 6.0 (`FailImmediatelyOnPermanentProviderErrorTests`, depende deste worker): exit 0, `total: 1`, `succeeded: 1` — sem regressão.
- HEAD e árvore de trabalho permaneceram estáveis (`git status --porcelain` idêntico) durante toda a revisão.

## Bloqueantes

- Nenhum.

## Recomendações

- Nenhuma nova. As recomendações não bloqueantes da revisão full (R1–R16) seguem pendentes de tratamento em outras tasks/PRDs, conforme já registrado em `prd_review.md`.

## Resultado

**VALIDAÇÃO APROVADA** — gate passou e os quatro bloqueantes atribuídos a esta task na reabertura (B1, B2, B3, B6) foram corrigidos sem regressão observada no escopo, incluindo o gate da Task 6.0 que depende deste worker.
