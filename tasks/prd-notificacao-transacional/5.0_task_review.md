# Revisão — Task 5.0 (revalidation)

Run: run.x5k8P8pa

- Tentativa: 3/3; branch `feature/notificacao-transacional`; HEAD `6f442819a2ca4aabb50f8558500290da30439a63`.
- Escopo: bloqueio anterior, diff novo, V-05, TechSpec, baseline G17 e regressões relevantes.

## Gate

- Gate declarado via `rtk proxy dotnet test ...RetryProviderTransientFailureTests ... --minimum-expected-tests 2`.
- Exit code 0; `total: 2`, `failed: 0`, `succeeded: 2`; `gate_expect` satisfeito. Gate: **passed**.

## Revalidação

- Bloqueio G17 corrigido: `TransactionalEmailDeliveryWorker` não abre mais transação antes do provedor;
  `ClaimNextAcceptedAsync` faz claim/lease em `UPDATE ... RETURNING` atômico, e a chamada HTTP ocorre após
  o comando terminar. O lock/transação não atravessa a entrega externa.
- Evidências: integração focalizada 2/2; integração completa 10/10; unidades focadas 10/10; unidades completas 13/13.
- Estado final, backoff crescente, outbox `notificacao.entrega-falhou.v1` e sinal único de tratamento manual
  continuam cobertos pelo teste de integração. HEAD e implementação permaneceram estáveis durante a revisão.

## Bloqueantes

- Nenhum.

## Recomendações

- A suíte arquitetural adicional saiu 7/8 (exit 2) por dependências preexistentes de workers em casos de uso;
  `TransactionalEmailDeliveryWorker` já tinha essa dependência no `HEAD`, portanto não é regressão desta task.
  Tratar a regra em task arquitetural dedicada.

## Resultado

**VALIDAÇÃO APROVADA** — gate passou e o bloqueio anterior foi resolvido sem regressão observada no escopo.
