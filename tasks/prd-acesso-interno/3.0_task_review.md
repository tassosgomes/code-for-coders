# Revisão da task 3.0: o ator interno entra, vê as áreas das suas permissões e sai

Run: run.ciaJ631w
Modo: focused · Tentativa: 1/3 · Data: 2026-09-25
HEAD: `28d50fcd2bd8090bc809bf0985c9806843f207d4`, com a árvore de trabalho não commitada da task
(hash do diff + untracked `ebeb315f…af0a7d`, igual no início e no fim da revisão)

**Resultado: VALIDAÇÃO REPROVADA**. 1 bloqueante e 5 recomendações.

## Gate

| Etapa | Resultado | Exit |
|---|---|---|
| Identity `StaffSessionTests` (`--minimum-expected-tests 5`) | 5/5 aprovados | 0 |
| BFF-admin E2E `StaffSessionTests` (`--minimum-expected-tests 5`) | 8/8 aprovados | 0 |
| admin-spa `vitest -t StaffSession` | 1 aprovado, 4 ignorados pelo filtro | 0 |

Foram 14 testes, acima do mínimo de 11 do `gate_expect`. O gate passou.

## Verificações do projeto

| Comando | Exit | Observação |
|---|---|---|
| `dotnet format` identity `--verify-no-changes` | 0 | |
| `dotnet build` identity | 0 | 0 warnings |
| Identity ArchitectureTests | 0 | 8/8 |
| Identity UnitTests | 0 | 15/15 |
| `dotnet format` bff-admin `--verify-no-changes` | 0 | |
| `dotnet build` bff-admin | 0 | 0 warnings |
| BFF-admin ArchitectureTests | 0 | 8/8 |
| BFF-admin UnitTests | 0 | 2/2 |
| admin-spa `lint` / `typecheck` / `build` | 0 / 0 / 0 | |
| bff-student EndToEndTests (regressão CAP-001) | 0 | 18/18 |

## Suítes completas dos componentes tocados

Rodei estas suítes porque a task mexe em testes e em telas existentes:

| Suíte | Exit | Resultado |
|---|---|---|
| `npm --prefix src/admin-spa run test` (é o passo de teste do `ci-react-ts.yml`, chamado por `admin-spa.yml`) | **1** | 4/5. **Falha `dashboard-screen.test.tsx`** |
| Identity IntegrationTests (suíte completa) | 2 | 37/38. Falha `IdentityInfrastructureTests.HeartbeatFlowsThroughOutboxRabbitMqAndConsumer`, **pré-existente** (ver abaixo) |
| BFF-admin EndToEndTests (suíte completa) | 0 | 11/11 |
| BFF-admin IntegrationTests | 0 | 1/1 |
| `dotnet ef migrations has-pending-model-changes` (Identity) | 0 | "No changes have been made to the model since the last migration." |

Para separar regressão de dívida anterior, rodei as mesmas suítes numa worktree temporária em HEAD
(`28d50fc`), já removida:

- admin-spa `src/features/admin-dashboard` em HEAD: 1/1 aprovado. **A falha foi introduzida pela 3.0.**
- Identity `IdentityInfrastructureTests` em HEAD: a mesma falha (`OptionsValidationException: Staff password
  reset URL must be absolute HTTP(S).`). É dívida da 2.0 e não bloqueia a 3.0. Ver R1.

## Bloqueante

### B1: a 3.0 quebrou a suíte de testes do admin-spa (check obrigatório de CI)

- Arquivo: `src/admin-spa/src/features/admin-dashboard/components/dashboard-screen.tsx:26-32`.
  Teste afetado: `dashboard-screen.test.tsx:12`.
- A task acrescentou `<p role="status">Sua conta ainda não tem acesso…</p>` quando `areas` está vazio.
  O padrão de `areas` é `[]`. Com isso, `DashboardScreen` passa a ter dois `role="status"`, e o
  `screen.getByRole('status')` do teste existente falha com
  `TestingLibraryElementError: Found multiple elements with the role "status"`.
- Impacto: `npm run test` sai com exit 1. O job `admin-spa` do CI (`ci-react-ts.yml@v1`, passo de teste
  com cobertura) reprova. A lista de verificações da task não inclui a suíte completa do SPA, e o
  filtro `-t StaffSession` do gate esconde a falha.
- Correção esperada: ajuste a tela (por exemplo, sem `role="status"` redundante) ou o teste existente,
  **sem enfraquecê-lo**. Ele precisa continuar provando o estado do serviço. Vale aproveitar e cobrir o
  critério "conta sem papel → orientação sem nenhuma área", que hoje não tem teste no SPA (ver R2).

## Conferências do contexto desta chamada

- **Testes antigos removidos:** nenhum arquivo de teste foi apagado. A regra de CSRF de cookie duplo e
  o `/bff/session` anônimo do esqueleto não tinham teste próprio no `bff-admin`. A nova
  `StaffSessionTests` do BFF cobre login, validação por ação, revogação, falha fechada, CSRF/origem,
  logout idempotente e proxy sem token.
- **bff-student:** não teve arquivo alterado. A suíte E2E passou 18/18. O teste de regressão
  `StudentSession_InternalActorIsRejectedWithInvalidCredentials` (Identity) passou.
- **Migração `AddStaffSessionIdempotency`:** o `.cs`, o `.Designer.cs` e o snapshot estão consistentes.
  `has-pending-model-changes` saiu com exit 0. A entidade `StaffSession`/`staff_sessions` já existia desde a
  2.0 (`AddStaffAccounts`). Esta migração só acrescenta `idempotency_records.staff_session_id`, e o
  resultado bate com a geração pelo EF.
- **Smoke em `localhost:8081`:** não foi executado. A stack Compose não foi subida, por decisão desta
  chamada. Fica como lacuna para a validação full.

## Revisão semântica (sem outros bloqueios)

- Identity autentica só `InternalActor` não desativado e verifica a senha contra um hash fictício quando
  não há credencial. Senha errada, e-mail inexistente e conta de aluno recebem a mesma 401
  `INVALID_CREDENTIALS`, e o teste compara status, código e título. Conta sem papel entra com
  `permissions: []`. A inatividade vem de `StaffSession:InactivityTimeoutMinutes=60`.
- `validateStaffSessionInternal` renova com `ExecuteUpdate` condicionado a sessão não revogada, não
  expirada e conta ativa, e devolve papéis e permissões vigentes.
- O middleware do BFF verifica Valkey e depois Identity em toda rota protegida. `SESSION_REQUIRED`
  remove Valkey e cookie. Qualquer outra falha vira 502/504 `IDENTITY_UNAVAILABLE` antes de
  `StoreAsync`, sem renovar o TTL (falha fechada, confirmada em teste).
- O CSRF agora fica vinculado à sessão (`OpaqueBffSession.CsrfToken`), com `FixedTimeEquals` e checagem
  de `Origin` em toda escrita com sessão, inclusive no logout.
- O proxy usa só o `AccessToken` da validação corrente e recusa com 403 quando ele não existe.
- O SPA tem `/admin/entrar` público, raiz protegida por loader (401 → redireciona), áreas derivadas de
  `permissions` e o botão Sair.

## Recomendações (não bloqueiam)

- **R1:** a falha de `HeartbeatFlowsThroughOutboxRabbitMqAndConsumer` é **pré-existente** desde a 2.0:
  falta `StaffAccount:PasswordResetBaseUrl` na fixture. Ela é um passo obrigatório do CI do
  `identity`, então vai reprovar a validação full se não for corrigida antes. Vale abrir a correção.
- **R2:** nenhum teste do SPA prova "conta sem papel vê a orientação" nem "áreas conforme
  `permissions`". Hoje só o login genérico está coberto.
- **R3:** o `ValkeyBffSessionStore` real não é exercitado com o novo formato de `OpaqueBffSession`,
  porque o E2E usa `InMemoryBffSessionStore`. O adaptador não mudou e a serialização é trivial, mas um
  teste de ida e volta nas IntegrationTests fecharia a lacuna.
- **R4:** no logout, se Identity falhar (502/504), o BFF já removeu Valkey e cookie, mas a sessão
  interna continua ativa em Identity até a inatividade expirar. A sessão fica inutilizável pelo
  navegador, mas vale registrar o comportamento na doc.
- **R5:** o POST anônimo `/api/v1/staff-sessions` não checa `Origin`. Isso abre risco de login CSRF.
  Vale comparar com o `bff-student` e alinhar se ele checar.

## Veredito

**VALIDAÇÃO REPROVADA.** O gate e todas as verificações listadas passaram. Mesmo assim, a task
introduziu uma regressão que quebra `npm run test` do admin-spa, que é um check obrigatório do CI
(B1). Recomendações: 5.

---

## Revalidação

Run: run.SCEWavaU

Modo: revalidation · Tentativa: 2/3 · Data: 2026-09-25  
Revisão anterior: run.ciaJ631w · Correções declaradas: run.HlYfAQKO

**Resultado da chamada: VALIDAÇÃO APROVADA.** 0 bloqueantes; 3 recomendações não bloqueantes
mantidas. O gate declarado passou.

### Gate da task

| Etapa | Resultado | Exit |
|---|---:|---:|
| Identity `StaffSessionTests` (`--minimum-expected-tests 5`) | 5/5 | 0 |
| BFF-admin E2E `StaffSessionTests` (`--minimum-expected-tests 5`) | 8/8 | 0 |
| admin-spa `vitest -t StaffSession` | 3 passaram, 4 filtrados | 0 |

Foram 16 testes no gate, acima do mínimo de 11 exigido por `gate_expect`.

### Verificações independentes

| Comando/check | Exit | Resultado |
|---|---:|---|
| `npm --prefix src/admin-spa run test` | 0 | 7/7 em 5 arquivos; cobertura de linhas 80,86% |
| Identity IntegrationTests completa | 0 | 38/38 |
| Identity `dotnet format ... --verify-no-changes` | 0 | sem alterações de formatação |
| Identity `dotnet build` | 0 | 0 warnings |
| Identity ArchitectureTests / UnitTests | 0 / 0 | 8/8 / 15/15 |
| BFF-admin `dotnet format ... --verify-no-changes` | 0 | sem alterações de formatação |
| BFF-admin `dotnet build` | 0 | 0 warnings |
| BFF-admin ArchitectureTests / UnitTests | 0 / 0 | 8/8 / 2/2 |
| admin-spa `lint` / `typecheck` / `build` | 0 / 0 / 0 | passaram; build mostrou dois avisos informativos do Vite |
| BFF-student EndToEndTests (regressão CAP-001) | 0 | 18/18 |

O build do SPA avisou que `/admin/runtime-env.js` não é incluído no bundle sem `type="module"`
e que um chunk minificado excede 500 kB; o build terminou com exit 0.

### Correções revalidadas

- **B1 resolvido:** `DashboardScreen` mantém apenas o status do serviço como `role="status"`
  ([dashboard-screen.tsx](../../src/admin-spa/src/features/admin-dashboard/components/dashboard-screen.tsx:45));
  a orientação de conta sem acesso é texto estático. A suíte completa passou, incluindo
  `dashboard-screen.test.tsx`.
- **R2 resolvido:** o teste de rota novo cobre permissões específicas e a conta sem papel,
  confirmando áreas visíveis, ausência de áreas não autorizadas, orientação e botão Sair
  ([admin-layout-route.test.tsx](../../src/admin-spa/src/app/routes/admin-layout-route.test.tsx:35)).
- **R1 resolvido:** a fixture do heartbeat agora configura `StaffAccount:PasswordResetBaseUrl` e
  usa banco dedicado criado por `MigrateAsync`
  ([IdentityInfrastructureTests.cs](../../src/identity/tests/CodeForCoders.Identity.IntegrationTests/IdentityInfrastructureTests.cs:22)).
  A suíte completa do Identity passou. A validação de produção permanece em
  `Identity.Infra.Data/DependencyInjection.cs:54-59`: exige URL absoluta HTTP(S), lifetime positiva
  e `ValidateOnStart`; o fix não a afrouxou.
- **R3–R5 mantidos como recomendações:** teste de serialização com `ValkeyBffSessionStore` real;
  documentar a sessão interna que permanece ativa quando o logout não alcança Identity; avaliar
  checagem de `Origin` no login anônimo. Não houve mudança relevante nesses pontos.

O smoke em `http://localhost:8081/admin/entrar` não foi executado: a decisão operacional desta
chamada determina que a stack Docker Compose não seja iniciada. Continua como lacuna para a
validação full, conforme o contexto recebido.

### Integridade da revisão

HEAD permaneceu `28d50fcd2bd8090bc809bf0985c9806843f207d4` e a árvore de HEAD permaneceu
`6aa66afae460884c02a500b0bca2b4023de7a0a2` durante os checks. O estado dos arquivos de código
permaneceu igual ao capturado no início da revisão; nenhum código, task, estado do fluxo ou commit
foi editado durante a validação.

**Veredito da chamada run.SCEWavaU: VALIDAÇÃO APROVADA.**


## Revalidação pós-full — cobertura CI

Run: run.rIJVXeXt

Modo: revalidation · Tentativa: 3/3 · Data: 2026-09-25  
HEAD revisado: `7411cf120489a9a5daea3df2007db4168f46c358` (igual ao início da chamada).

**Resultado: VALIDAÇÃO APROVADA.** B1 resolvido; 0 bloqueantes novos. As 3 recomendações não bloqueantes da revisão anterior permanecem sem mudança relevante.

### B1 — cobertura agregada do CI

Executei `dotnet restore` e a suíte integral de cada componente com `dotnet test --no-restore --configuration Debug --coverage --coverage-output-format cobertura`. Cada execução gerou quatro relatórios Cobertura e passou. A agregação reproduz a união de linhas (arquivo + número, coberta se qualquer relatório tiver hits) do `ci-dotnet.yml@v1`, acionado por `.github/workflows/{bff-admin,identity,notification}.yml`, limite 70%.

| Componente | Testes | União de linhas cobertas/total | Cobertura |
|---|---:|---:|---:|
| bff-admin | 43/43 | 1.987/2.547 | **78,01%** |
| identity | 109/109 | 8.651/11.008 | **78,59%** |
| notification | 51/51 | 3.514/4.116 | **85,37%** |

Havia relatórios antigos locais em `TestResults`; a conta acima usou somente os quatro IDs recém-gerados por serviço, como numa execução limpa do CI. Nenhum dos 12 relatórios atuais contém linhas de `obj/`, `*.g.cs` ou `*.generated.cs`. O `bff-admin` supera o limite por 8,01 pontos percentuais, resolvendo B1 (67,95% < 70%) da validação full. A medição independente de Identity foi 78,59%; o relatório do implementer indicou 78,60%, diferença de 0,01 ponto sem efeito no limite.

Comparei `Directory.Build.props` e `testconfig.json` de `bff-admin`, `identity` e `notification` com `src/bff-student/tests`, normalizando somente o nome do componente. Os seis arquivos deram MATCH; as exclusões são `obj/` e `*.g.cs`/`*.generated.cs`. Os arquivos `.testconfig.json` correspondentes foram copiados para os 12 diretórios de saída dos projetos de teste.

### Gate e verificações

| Verificação | Resultado |
|---|---|
| Gate da task: Identity StaffSession 5/5; BFF StaffSession 8/8; SPA StaffSession 3 aprovados | exit 0; 16 aprovados (mínimo 11) |
| `dotnet format --verify-no-changes` — identity, bff-admin, notification | 0 / 0 / 0 |
| Build — identity, bff-admin, notification | 0 / 0 / 0; zero warnings novos |
| Identity ArchitectureTests / UnitTests | 8/8 / 15/15; exit 0 |
| BFF-admin ArchitectureTests / UnitTests | 8/8 / 2/2; exit 0 |
| Admin SPA lint / typecheck / build | 0 / 0 / 0 |
| BFF-student EndToEndTests (regressão) | 18/18; exit 0 |

O build da SPA manteve os avisos informativos já registrados sobre `runtime-env.js` e tamanho do chunk. A suíte completa da SPA passou na revalidação anterior (7/7); não houve mudança de SPA nesta tentativa. Builds e testes integrais das três solutions foram executados nesta chamada.

A correção cobre somente B1 da validação full. B2–B5 de `prd_review.md` continuam registrados e exigem tratamento no fluxo full; não foram reavaliados aqui. O diff desta tentativa contém somente os seis arquivos de configuração de cobertura para o escopo de implementação. HEAD permaneceu igual; nenhum arquivo de código foi alterado durante os checks. `3_task.md` e `flow-state.json` já estavam modificados no início pela operação do fluxo e não foram editados nesta revisão.

**Veredito da chamada run.rIJVXeXt: VALIDAÇÃO APROVADA.**
