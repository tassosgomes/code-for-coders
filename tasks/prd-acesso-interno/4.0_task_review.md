# Revisão da task 4.0 — Administrador convida e o convidado recebe o e-mail

Run: run.vU1d8ECY
Modo: focused · Tentativa: 1/3 · Data: 2026-09-25
HEAD: `5d412ec0da25b8b55315c3d6c1c794576a258f19` (checkpoint 3.0) + árvore de trabalho não commitada
Hash do `git diff` (início e fim da revisão, iguais): `4615975474516d04bac4b527f0f9bd6f4764898575762dc95eb60598ab2e3c6a`

## Resultado: VALIDAÇÃO REPROVADA

1 bloqueante · 4 recomendações. Gate e todos os checks passaram; a reprovação vem de uma quebra de contrato
que o gate não cobre.

## Gate

Comando: o `gate` do frontmatter, sem alteração (via `rtk proxy`). **Exit 0.**

| Parte | Resultado |
|---|---|
| Identity `StaffInvitationIssuingTests` (`--minimum-expected-tests 7`) | 7/7 |
| Notification `StaffInvitationEmailTests` (min 3) | 3/3 |
| BFF `StaffInvitationIssuingTests` (min 3) | 3/3 |
| SPA `-t StaffInvitationIssuing` | 4 passaram, 7 ignorados pelo filtro |

`gate_expect` (≥ 14: 7+3+3+1): atendido (17).

## Verificações do projeto e suítes completas

Todos em primeiro plano, exit 0:

| Componente | Check | Resultado |
|---|---|---|
| identity | `dotnet format --verify-no-changes` | 0 |
| identity | `dotnet build` slnx | 0, 0 warnings |
| identity | ArchitectureTests / UnitTests / IntegrationTests (completa) | 8/8 · 15/15 · 45/45 |
| notification | format / build | 0 · 0 warnings |
| notification | UnitTests / IntegrationTests (completa) | 20/20 · 22/22 |
| bff-admin | format / build | 0 · 0 warnings |
| bff-admin | ArchitectureTests / UnitTests / EndToEndTests (completa) | 8/8 · 2/2 · 14/14 |
| admin-spa | lint / typecheck / build / `npm run test` completo | 0 · 0 · 0 · 11/11 |
| identity | `dotnet ef migrations has-pending-model-changes` | 0 — sem mudança pendente |
| notification | `dotnet ef migrations has-pending-model-changes` | 0 — sem mudança pendente |

Migrations: `20260925183745_AddStaffInvitations` pertence ao `IdentityDbContext` (tabela `staff_invitations`,
índice único parcial `ux_staff_invitations_pending_email` com `accepted_on IS NULL AND superseded_on IS NULL`,
colunas de resultado em `idempotency_records`); `20260925183757_AddStaffInvitationRole` pertence ao
`NotificationDbContext` (`delivery_records.recipient_role`). Ambas têm Designer e snapshot atualizados,
formato de saída do EF.

Não executado (conforme `context.txt`): smoke via Docker Compose — fica para a full.

## Conformidade verificada

- **Notificação 1.1.0** (`asyncapi-contract-notification.yaml`): `convite-interno` aceito como finalidade e modelo,
  `papel`+`link` obrigatórios, `nome` ausente; `confirmacao-de-conta`/`recuperacao-de-senha` continuam exigindo
  `nome`; `Nome`/`Papel` omitidos quando nulos na serialização; `ValidityHoursByPurpose["convite-interno"]`
  validado na partida (`ValidateOnStart`) e = 168 no appsettings.
- **Ato `auditoria.ato-praticado.v1`**: 9 campos (`fatoId, origem, tipo, tenantId, praticadoEm, autor, alvo,
  complemento, motivo`), `origem=identidade`, `tipo=convite-interno-emitido`, autor `conta-interna`, alvo
  `convite-interno`, `complemento.papel`, sem e-mail; exchange `AuditExchange`; payload protegido. Teste confirma.
- Pedido e ato gravados no mesmo commit do convite; `fatoId`/`pedidoId` = id da mensagem no outbox (reentrega repete).
- Substituição: convite anterior recebe `superseded_on`, novo devolve `supersededInvitationId`; dois atos e dois pedidos.
- 422 `EMAIL_BELONGS_TO_STAFF` / `EMAIL_BELONGS_TO_STUDENT` / `REASON_REQUIRED` sem convite, outbox nem registro de idempotência.
- 403 `PERMISSION_DENIED`: em Identity, checado no endpoint antes do caso de uso; no BFF, antes de chamar Identity (teste E2E).
- Escopos `staff-invitations:read`/`:write` e operationIds conforme `internal-api-contract.yaml`.

## Bloqueante

### B-01 — Motivo aceita até 2000 caracteres; contratos limitam a 1000 e o ato sai não conforme

- `src/identity/src/CodeForCoders.Identity.Application/UseCases/Accounts/CreateStaffInvitation/CreateStaffInvitationInputValidator.cs:17`
  — `RuleFor(input => input.Reason).MaximumLength(2000)`.
- `src/admin-spa/src/features/staff-access/api/staff-invitations.ts:12` — `z.string().trim().min(1, …).max(2000)`.
- Contratos: `Reason` com `maxLength: 1000` em `internal-api-contract.yaml:487`, `api-contract.yaml:961`,
  `asyncapi-contract.yaml:252` (`AtoPraticadoIdentidade.motivo`) e `motivo.maxLength: 1000` em
  `AtoPraticadoPayload` da Auditoria (`tasks/prd-trilha-auditoria/asyncapi-contract.yaml:177-180`).
- Falha concreta: administrador envia motivo com 1500 caracteres → SPA aceita, BFF repassa, Identity devolve 201
  e grava o ato `convite-interno-emitido` com `motivo` de 1500 caracteres → o payload viola `AtoPraticadoPayload`
  e a Auditoria registra o ato como **não conforme**. Quebra o item "Payload do ato gravado valida contra
  `AtoPraticadoPayload` da Auditoria 1.0.1 e contra `AtoPraticadoIdentidade`" do *Pronto quando* e o contrato
  interno/público (entrada > 1000 deveria ser recusada como 400).
- Correção esperada: limitar a 1000 (Identity e SPA; conferir BFF se validar) e cobrir com teste que um motivo
  de 1001 caracteres é recusado sem gravar convite, pedido nem ato.

## Recomendações (não bloqueiam)

1. **Corrida no índice parcial**: dois convites simultâneos ao mesmo e-mail com chaves de idempotência distintas
   violam `ux_staff_invitations_pending_email`; `CreateStaffInvitation` só trata o conflito de
   `ux_idempotency_records_scope_key`, então o perdedor tende a 500. Tratar como conflito/nova tentativa.
2. **`ROLE_NOT_SUPPORTED`** (`CreateStaffInvitation.cs:45-48`) devolve 422 com código fora da lista de
   `BusinessRule`; pelo schema `Role` (enum) o caso é 400 `INVALID_REQUEST`.
3. Não há teste de Identity para o 403 sem efeitos (a garantia vem da ordem no endpoint e do E2E do BFF);
   nem teste SPA do professor abrindo a rota "Acessos" e vendo "sem permissão".
4. A lista de pendentes exclui convites expirados; alinhado ao sentido de "pendente", mas vale registrar
   a decisão na spec se a 6.0 depender do comportamento.

## Escopo revisado

Diff desde `5d412ec` (42 arquivos modificados) e untracked do escopo em identity, notification, bff-admin e
admin-spa; `4_task.md`/`flow-state.json` só com mudança de status do orquestrador. Relatório do implementer
(`run.KqWpvtYb`) usado apenas como alegação; os resultados acima foram reexecutados nesta revisão.

## Revalidação — task 4.0

Run: run.mpvAo9ro

Modo: revalidation · Tentativa: 2/3 · Data: 2026-09-25  
HEAD revisado: `5d412ec0da25b8b55315c3d6c1c794576a258f19` (`feature/acesso-interno`)

### Resultado desta chamada: VALIDAÇÃO APROVADA

0 bloqueantes remanescentes. As 4 recomendações da revisão anterior continuam não bloqueantes e não foram
alteradas pela correção.

### Gate declarado

Executado sem alteração do comando do frontmatter, via `rtk proxy`; **exit 0**.

| Parte | Resultado |
|---|---|
| Identity `StaffInvitationIssuingTests` | 8 passaram (mín. 7) |
| Notificação `StaffInvitationEmailTests` | 3 passaram (mín. 3) |
| BFF `StaffInvitationIssuingTests` | 3 passaram (mín. 3) |
| SPA `StaffInvitationIssuing` | 5 passaram, 7 ignorados pelo filtro |

`gate_expect` atendido: 19 testes passaram, acima do mínimo de 14.

### Verificações do projeto e suítes completas

Todos os comandos terminaram com exit 0:

| Componente | Checks | Resultado |
|---|---|---|
| Identity | `dotnet format ... --verify-no-changes`; `dotnet build`; ArchitectureTests; UnitTests; IntegrationTests completas | Format OK; build com 0 warnings; 8, 15 e 46 testes passaram |
| Notificação | `dotnet format ... --verify-no-changes`; `dotnet build`; UnitTests; IntegrationTests completas | Format OK; build com 0 warnings; 20 e 22 testes passaram |
| BFF Admin | `dotnet format ... --verify-no-changes`; `dotnet build`; ArchitectureTests; UnitTests; EndToEndTests completas | Format OK; build com 0 warnings; 8, 2 e 14 testes passaram |
| Admin SPA | `npm --prefix src/admin-spa run lint`; `typecheck`; `build`; `test` completos | Todos OK; 12 testes passaram |

O build da SPA terminou com avisos do Vite sobre `runtime-env.js` sem `type="module"` e chunk acima de
500 kB; não afetaram o exit code. O aviso do plugin React também apareceu nos testes.

### Revalidação do bloqueante B-01

- Resolvido: Identity limita `Reason` a 1000 caracteres em
  `CreateStaffInvitationInputValidator.cs:17`; o schema da SPA aplica o mesmo limite em
  `staff-invitations.ts:12`. O contrato público continua em `api-contract.yaml:961`.
- `StaffInvitation_RejectsAReasonLongerThanTheContractLimitWithoutWrites` envia 1001 caracteres, verifica
  erro de validação no campo `Reason` e confirma ausência de convite, mensagens no outbox e registro de
  idempotência (`StaffInvitationIssuingTests.cs:171-184`). O caso de uso valida antes de iniciar gravações;
  o `GlobalExceptionHandler` mapeia `ValidationException` para 400 `INVALID_REQUEST`.
- O teste SPA envia 1001 caracteres à interface, verifica a mensagem de limite e confirma que nenhum POST
  é enviado (`staff-access-route.test.tsx`). O BFF apenas repassa o motivo a Identity, sem validação própria.
- Sem regressões observadas nos gates e suítes completas executados.

### Limites e recomendações mantidas

O smoke via Docker Compose não foi executado, conforme a decisão operacional em `context.txt`; fica para a
validação full. Permanecem as quatro recomendações da revisão anterior: corrida no índice único parcial;
`ROLE_NOT_SUPPORTED` fora do contrato; cobertura de permissão 403 em Identity/SPA; e decisão documentada
sobre exclusão de convites expirados da lista de pendentes.

HEAD permaneceu em `5d412ec0da25b8b55315c3d6c1c794576a258f19`; os checks não introduziram novos caminhos
alterados na árvore de trabalho.

## Revalidação — task 4.0 (tentativa 3/3)

Run: run.gWMl0Sfm

Modo: revalidation · Data: 2026-09-25  
HEAD revisado: `986d740edda6bf11bfb337ad1aba5343b9ea9881` (`feature/acesso-interno`)

### Resultado desta chamada: VALIDAÇÃO APROVADA

0 bloqueantes remanescentes para a task 4.0. O bloqueante B5 do relatório full (`prd_review.md`,
`run.QeuEMpDO`) foi corrigido com um teste HTTP no Identity. Esta aprovação é da task; o relatório
full ainda registra B1–B4, que não pertencem a esta revalidação.

### Gate declarado

Executado sem alterar o comando do frontmatter, via `rtk proxy`; **exit 0**. Passaram 19 testes, acima
do mínimo de 14 de `gate_expect`:

| Parte | Resultado |
|---|---|
| Identity `StaffInvitationIssuingTests` | 8 passaram (mín. 7) |
| Notificação `StaffInvitationEmailTests` | 3 passaram (mín. 3) |
| BFF `StaffInvitationIssuingTests` | 3 passaram (mín. 3) |
| SPA `StaffInvitationIssuing` | 5 passaram; 21 ignorados pelo filtro |

### Verificações do Identity

Todos os comandos terminaram com exit 0:

| Check | Resultado |
|---|---|
| `dotnet format src/identity/CodeForCoders.Identity.slnx --verify-no-changes` | OK |
| `dotnet build src/identity/CodeForCoders.Identity.slnx` | OK, 0 warnings |
| ArchitectureTests completa | 8/8 |
| UnitTests completa | 15/15 |
| IntegrationTests completa | 80/80 |
| EndToEndTests completa | 8/8, incluindo os dois novos casos |

### Revalidação do B5 — autorização `acesso.gerir`

- `StaffInvitationIssuingEndpointTests.cs` exercita o host real do Identity com sessão de professor e
  token de serviço válido. Sem `acesso.gerir`, verifica 403 `PERMISSION_DENIED`, Problem Details e que
  convites, outbox e registros de idempotência não mudaram. O caso de controle com administrador
  confirma a emissão válida (201 e duas mensagens na outbox).
- Sensor em worktree temporária no mesmo HEAD, com o teste novo copiado para lá: neutralizei a guarda
  de `StaffInvitationEndpoints.cs:136`, substituindo a permissão exigida por `__mutant__`. O teste
  `CreateStaffInvitationInternal_RejectsASessionWithoutManageAccessWithoutWrites` falhou como
  esperado: **esperava Forbidden, recebeu Created** (linha 32); o controle positivo passou. O runner
  terminou com exit 2, evidência de que o mutante foi morto.
- A worktree temporária foi removida. HEAD e `git status --porcelain` do checkout original ficaram
  iguais ao baseline capturado antes da mutação (`986d740...`; as mesmas alterações do orquestrador em
  `4_task.md`/`flow-state.json` e o teste novo não rastreado).

### Recomendações e escopo

Sem regressões ou novas recomendações nesta revalidação. A recomendação de cobertura para o 403 no
Identity foi resolvida; permanece a recomendação separada de teste da rota SPA sem permissão. Também
continuam as recomendações anteriores sobre corrida no índice parcial, `ROLE_NOT_SUPPORTED` fora do
contrato e a decisão sobre convites expirados na listagem. O smoke via Docker Compose não foi repetido;
esta chamada revalida B5 e os gates/suítes indicados no contexto operacional.
