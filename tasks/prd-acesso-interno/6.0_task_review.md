# Revisão da task 6.0 — Administrador concede e revoga papel, e a revogação desconecta na hora

Run: run.NCVira28
Modo: focused · Tentativa: 1/3 · Data: 2026-09-25
HEAD revisado: `cc581e2ca6df3fcd47b5d9ccccf57c3f9a6c62af` (checkpoint 5.0) + working tree da task (20 modificados, 21 untracked)
Árvore inalterada durante a revisão (HEAD e `git status --porcelain` conferidos antes e depois).

## Resultado

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 4 recomendações.

## Gate

| Comando | Exit | Resultado |
|---|---|---|
| `dotnet test … Identity.IntegrationTests … --filter-class …StaffRoleGrantRevokeTests --minimum-expected-tests 8` | 0 | 10/10 passaram |
| `dotnet test … BffAdmin.EndToEndTests … --filter-class …StaffRoleGrantRevokeTests --minimum-expected-tests 4` | 0 | 7/7 passaram |
| `npm --prefix src/admin-spa run test -- -t StaffMembers` | 0 | 4 passaram (15 skipped por filtro) |

`gate_expect` (≥ 13: 8 Identity, 4 BFF, 1 SPA): atendido com 21 (10 + 7 + 4). Comandos executados via `rtk proxy`, em primeiro plano.

## Verificações do projeto

| Componente | Comando | Exit | Observação |
|---|---|---|---|
| identity | `dotnet format …Identity.slnx --verify-no-changes` | 0 | |
| identity | `dotnet build …Identity.slnx` | 0 | 0 warnings |
| identity | ArchitectureTests | 0 | 8/8 |
| identity | UnitTests | 0 | 15/15 |
| bff-admin | `dotnet format …BffAdmin.slnx --verify-no-changes` | 0 | |
| bff-admin | `dotnet build …BffAdmin.slnx` | 0 | 0 warnings |
| bff-admin | ArchitectureTests | 0 | 8/8 |
| bff-admin | UnitTests | 0 | 2/2 |
| admin-spa | `npm run lint` | 0 | |
| admin-spa | `npm run typecheck` | 0 | |
| admin-spa | `npm run build` | 0 | |

Suítes completas dos componentes tocados (pedido do orquestrador):

| Suíte | Exit | Resultado |
|---|---|---|
| Identity.IntegrationTests (completa) | 0 | 66/66 |
| Identity.EndToEndTests | 0 | 6/6 |
| BffAdmin.EndToEndTests (completa) | 0 | 25/25 |
| BffAdmin.IntegrationTests | 0 | 1/1 |
| admin-spa `npm run test` (completa) | 0 | 19/19 em 7 arquivos |

Migração: `dotnet ef migrations has-pending-model-changes --project …Infra.Data --startup-project …Identity.Api` → exit 0, "No changes have been made to the model since the last migration". `20260925204842_AddStaffRoleActionIdempotencyResult` (coluna nullable `staff_role_action_changed`) com Designer e snapshot consistentes, formato do gerador EF.

Não executados (fora do escopo focused, conforme task e contexto): cobertura agregada ≥ 70%, `dotnet publish`, imagem, Docker Compose e smoke com dois navegadores — ficam para a validação full.

## Conformidade verificada

- **Contratos HTTP interno/público:** rotas `/internal/v1/staff-members`, `…/role-grants`, `…/role-revocations` e as públicas `/api/v1/…` batem com `internal-api-contract.yaml`/`api-contract.yaml`; escopos `staff-members:read|write` incluídos em `ServiceAssertionTokenFactory` e exigidos pelo Identity; `Role` restrito ao enum (`professor|suporte|financeiro|administrador`) no BFF, Identity e SPA (zod); `Reason` ≤ 1000 em todas as camadas, só-espaços → 422 `REASON_REQUIRED`; `Idempotency-Key` ≤ 128; `size` ≤ 100; `StaffMember.roles` validado como único e ≤ 4 na SPA.
- **Códigos de erro:** `SELF_ROLE_CHANGE_FORBIDDEN` (422), `STAFF_MEMBER_NOT_FOUND` (404, inexistente/aluno/outro tenant via `FindInternalAccountAsync` escopado por tenant), `PERMISSION_DENIED` (403, `acesso.gerir` checado no BFF antes de chamar Identity e de novo no Identity), `IDEMPOTENCY_CONFLICT` → `IDEMPOTENCY_KEY_REUSED` no BFF (conforme nota do contrato interno), `SESSION_REQUIRED` (401).
- **Auditoria:** `StaffRoleAuditFactV1` gera `origem=identidade`, `tipo=papel-concedido|papel-revogado`, autor/alvo `conta-interna`, `complemento.papel`, `motivo`, `fatoId` v7 novo por ato; o teste de integração desserializa o payload do outbox e passa pelo `AdministrativeActPolicy.GetNonConformityReasons` da Auditoria sem razões de não conformidade — valida contra `AtoPraticadoPayload` com o domínio real da Auditoria.
- **Atomicidade (ADR-0005 (2)):** revogação remove a atribuição, revoga todas as `StaffSession` não revogadas do alvo e grava o ato no mesmo `CommitAsync`; teste confirma sessão com `RevokedOn` e um ato.
- **Sem efeito / sobre si:** `changed: false` sem outbox; autor = alvo rejeitado antes de qualquer escrita (`AssertNoSideEffectsAsync`).
- **Concorrência:** duas concessões do mesmo papel com barreira real em PostgreSQL → uma atribuição, um ato, duas respostas (`changed` true/false). Conflito em `ux_staff_role_assignments_tenant_account_role` tratado; `IdentityUnitOfWork` limpa o `ChangeTracker` antes do reprocesso, então o segundo commit não reenvia a atribuição/ato. Revogação concorrente resolvida via `DbUpdateConcurrencyException` → no-op.
- **RN-13/RF-08 (desconexão):** Identity revoga as sessões; o BFF revalida a sessão no Identity a cada requisição e devolve 401 `SESSION_REQUIRED` quando Identity a recusa (teste E2E do BFF).
- **SPA:** lista com papéis, "(você)" e sem ações na linha `isSelf`; Conceder/Revogar exigem motivo (validação antes do envio), aviso "a pessoa será desconectada agora", `Idempotency-Key` estável por tentativa idêntica e renovado após sucesso; invalidação da query após mutação.
- **Adaptador real:** `StaffMemberIdentityClient` (HttpClient tipado com resiliência e retry desabilitado para POST) é exercitado nos E2E do BFF pela aplicação real (`WebApplicationFactory`), com Identity simulado no `HttpMessageHandler`.

## Bloqueantes

Nenhum.

## Recomendações (não bloqueiam)

1. **Integração BFF + Identity da desconexão é costurada, não ponta a ponta.** `src/bff-admin/tests/CodeForCoders.BffAdmin.EndToEndTests/StaffRoleGrantRevokeTests.cs:130` marca manualmente `RevokedSessionIds` no fake de Identity após a revogação; a ligação real (Identity revoga → `validate` do Identity recusa) é coberta só pelo lado Identity. O item "Integração BFF + Identity" do *Pronto quando* fica demonstrado por composição; confirmar no smoke com dois navegadores da validação full.
2. **Falta teste de revogação sem efeito.** Não há caso para "papel ausente → `changed: false`, nada gravado, sessões intactas" em `src/identity/tests/CodeForCoders.Identity.IntegrationTests/StaffRoleGrantRevokeTests.cs` (existe só a variante de concessão, linha 105). O caminho é simétrico em `StaffRoleActionExecutor.cs`, mas um teste evitaria regressão que revogue sessões num no-op.
3. **Contrato interno de listagem não declara 400.** `StaffMemberEndpoints.cs` (Identity) devolve 400 `INVALID_REQUEST` para `page/size` inválidos ou `X-Staff-Session` ausente, mas `internal-api-contract.yaml:249-255` só lista 200/401/403 para `listStaffMembersInternal`. Alinhar contrato ou comportamento via contract-creator.
4. **`ROLE_NOT_SUPPORTED` é inalcançável.** O endpoint do Identity já responde 400 para papel fora do catálogo antes do use case; o ramo 422 em `StaffRoleActionExecutor.cs` só protege chamadas diretas. Aceitável como defesa em profundidade; registrar que o código não é público.

## Escopo revisado

Identity (Api/Application/Domain/Infra.Data/Contracts, migração, testes de integração), BFF-admin (endpoints, cliente tipado, contratos, escopos, E2E), admin-spa (`staff-members.ts`, `staff-access-screen.tsx`, testes de rota, handlers MSW). `flow-state.json` e `6_task.md` não revisados como código (pertencem ao orquestrador).
