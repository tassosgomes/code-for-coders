# Revisão focused — Task 5.0 Convidado aceita o convite e entra no backoffice

Run: run.OqoExtC1
Modo: focused · Tentativa: 1/3 · Data: 2026-09-25
Base (checkpoint 4.0): `bbe4eff97ce2e54e7960060543a8664010380fb7` (HEAD inalterado durante a revisão; árvore de trabalho igual no início e no fim)

## Resultado

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 4 recomendações.

## Gate declarado

| Parte | Exit | Evidência |
|---|---|---|
| Identity `StaffInvitationAcceptanceTests` (`--minimum-expected-tests 7`) | 0 | 10/10 aprovados |
| BFF `StaffInvitationAcceptanceTests` (`--minimum-expected-tests 3`) | 0 | 4/4 aprovados |
| SPA `vitest -t StaffInvitationAcceptance` | 0 | 3 aprovados (1 arquivo) |

`gate_expect` (≥ 11: 7 Identity, 3 BFF, 1 SPA): cumprido com 17 (10 + 4 + 3).

## Verificações do projeto

| Componente | Comando | Exit |
|---|---|---|
| identity | `dotnet format …Identity.slnx --verify-no-changes` | 0 |
| identity | `dotnet build …Identity.slnx` | 0 (0 warnings) |
| identity | ArchitectureTests | 0 (8/8) |
| identity | UnitTests | 0 (15/15) |
| bff-admin | `dotnet format …BffAdmin.slnx --verify-no-changes` | 0 |
| bff-admin | `dotnet build …BffAdmin.slnx` | 0 (0 warnings) |
| bff-admin | ArchitectureTests | 0 (8/8) |
| bff-admin | UnitTests | 0 (2/2) |
| admin-spa | `npm run lint` / `typecheck` / `build` | 0 / 0 / 0 |

Suítes completas dos componentes tocados (pedido do contexto do orquestrador):
Identity IntegrationTests 56/56, Identity EndToEndTests 6/6, BFF EndToEndTests 18/18,
BFF IntegrationTests 1/1, SPA `vitest run` 15/15 (7 arquivos). Os projetos `*.Tests.Common`
saem 1 com "No test projects were found" — são bibliotecas de apoio, não suítes.

Não executados (fora do escopo focused, conforme task/contexto): smoke com Docker Compose,
cobertura agregada ≥ 70%, `dotnet publish` e build de imagem — ficam para a full.

## Escopo revisado

- Identity: `AcceptStaffInvitation` e `LookupStaffInvitation` (use cases, validadores, DI),
  `StaffInvitation.Accept`, `FindByTokenHashAsync`, `AddCredential`, `AppendInvitationAcceptedAsync`,
  `StaffInvitationAcceptedAuditFactV1`, endpoints internos e testes de integração.
- BFF: `StaffInvitationIdentityClient` (lookup/accept), endpoints públicos, `BffSecurityMiddleware`
  (rotas sem sessão + Origin obrigatório no aceite), contratos e testes E2E.
- SPA: rota `/convite`, API `staff-invitation-acceptance.ts`, tela, handlers MSW e teste.

## Conformidade verificada

- **Contratos HTTP (público e interno):** `token` 1..512, `name` 1..200, `Idempotency-Key` 1..128
  validados no BFF, na API do Identity e no FluentValidation; `InvitationPreview` = `{offeredRole, expiresAt}`
  sem e-mail/convidante; resposta de aceite = `StaffSession` com `csrfToken` e cookie `staff_session`.
  Códigos 422 `INVITATION_INVALID`, `INVITATION_EMAIL_UNAVAILABLE`, `PASSWORD_POLICY_VIOLATION`
  (com `errors.password`) e `IDEMPOTENCY_CONFLICT` interno → `IDEMPOTENCY_KEY_REUSED` público. Escopos
  `staff-invitations:read` (lookup) e `:write` (aceite).
- **RN-15 / RF-05:** expirado, aceito, substituído e inexistente dão o mesmo 422 no lookup e no aceite
  (testes com relógio controlado). Segundo uso → `INVITATION_INVALID`.
- **Commit único:** conta `InternalActor` confirmada, credencial PBKDF2, atribuição do papel ofertado,
  convite aceito, sessão interna, registro de idempotência e outbox num único `CommitAsync`.
  Senha fraca e nome acima do limite não gravam nada (testado).
- **Ato de auditoria:** exatamente um `auditoria.ato-praticado.v1`, `tipo=convite-interno-aceito`,
  autor `conta-interna`/conta nova, alvo `convite-interno`/convite, 7 campos, sem `motivo` nem
  `complemento`; nenhum `papel-concedido` nem notificação. Conferido contra o exemplo
  `conviteInternoAceito` do AsyncAPI e contra `AdministrativeActPolicy` da Auditoria
  (`convite-interno-aceito` aceito sem motivo; referências em kebab-case válidas).
- **Corrida de e-mail:** unicidade `ux_accounts_tenant_id_normalized_email` → `ResolveWriteConflictAsync`;
  `IdentityUnitOfWork` limpa o `ChangeTracker` antes do re-read, então o convite relido reflete o banco.
- **Segurança:** token só no corpo, hash SHA-256 igual ao da emissão (4.0); SPA remove o token do
  histórico (`replaceState`) antes da primeira pintura; aceite exige Origin permitido (403
  `CSRF_INVALID`, testado); cookie/CSRF opacos com 32 bytes aleatórios; sessão BFF gravada no store.
- **Link:** `AcceptanceBaseUrl` = `/admin/convite`; rota SPA `/convite` sob `basename` `/admin`.

## Bloqueantes

Nenhum.

## Recomendações (não bloqueiam)

1. `src/admin-spa/src/features/staff-access/api/staff-invitation-acceptance.ts:84` — a
   `Idempotency-Key` é gerada a cada envio; um reenvio após falha de rede não reaproveita a chave
   e cai em `INVITATION_INVALID` se o primeiro aceite já tiver sido gravado. Considere manter a
   chave estável por tentativa de formulário.
2. `src/identity/tests/.../StaffInvitationAcceptanceTests.cs:118-124` — o teste confere o payload
   campo a campo; desserializar com `AtoPraticadoJsonConverter` e aplicar `AdministrativeActPolicy`
   daria prova direta de conformidade com a Auditoria.
3. A task lista `EndpointExtensions.cs` (Identity e BFF) como arquivos a modificar; o mapeamento foi
   feito nos `StaffInvitationEndpoints` já registrados — equivalente, sem ação necessária além de
   ajustar a expectativa em tasks futuras.
4. Item "Smoke" do "Pronto quando" continua pendente e deve ser coberto na validação full.
