# Revisão focused — Task 9.0 Ator interno recupera a senha sozinho

Run: run.lAGdudjh
Modo: focused · Tentativa: 1/3 · Data: 2026-09-25
HEAD: `868d173bda9ebfc5c03e479dae5bbff579b5916d` (mudanças da task na árvore de trabalho, não commitadas)
Branch: `feature/acesso-interno`

## Resultado

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 4 recomendações.

## Gate

Comando declarado no frontmatter, executado sem alteração (`rtk proxy`):

| Etapa | Resultado |
|---|---|
| Identity `StaffPasswordRecoveryRequestTests` (`--minimum-expected-tests 3`) | 3/3 aprovados |
| BFF `StaffPasswordRecoveryRequestTests` (`--minimum-expected-tests 2`) | 2/2 aprovados |
| SPA `vitest -t StaffPasswordRecovery` | 2 aprovados, 24 ignorados pelo filtro |
| **Exit code** | **0** |

`gate_expect` (≥ 6: 3 Identity, 2 BFF, 1 SPA): atendido com 7 testes.

## Verificações do projeto e suítes completas

| Componente | Comando | Exit | Observação |
|---|---|---|---|
| identity | `dotnet format …Identity.slnx --verify-no-changes` | 0 | |
| identity | `dotnet build …Identity.slnx` | 0 | 0 warnings |
| identity | ArchitectureTests | 0 | 8/8 |
| identity | UnitTests | 0 | 15/15 |
| identity | IntegrationTests (suíte completa) | 0 | 79/79 |
| bff-admin | `dotnet format …BffAdmin.slnx --verify-no-changes` | 0 | |
| bff-admin | `dotnet build …BffAdmin.slnx` | 0 | 0 warnings |
| bff-admin | ArchitectureTests | 0 | 8/8 |
| bff-admin | UnitTests | 0 | 2/2 |
| bff-admin | EndToEndTests (suíte completa) | 0 | 32/32 |
| admin-spa | `npm run lint` | 0 | |
| admin-spa | `npm run typecheck` | 0 | |
| admin-spa | `npm run build` | 0 | |
| admin-spa | `npm run test` (suíte completa) | 0 | 26/26 em 10 arquivos |

Docker Compose / smoke com smtp4dev não foram executados, conforme o contexto da chamada (fica para a full).
Cobertura agregada, `dotnet publish` e imagem ficam para a full, como declara a task.

## Escopo revisado

- Diff desde o checkpoint `868d173` e todos os arquivos untracked da task (Identity: endpoint interno, caso de
  uso `RequestStaffPasswordReset`, validator, store; BFF: endpoint, cliente, contrato; SPA: rota, tela, API,
  link na entrada, handler MSW, testes).
- `9_task.md`: só o `status` mudou (orquestrador). `flow-state.json` não revisado (pertence ao orquestrador).
- Contratos aprovados (`api-contract.yaml`, `internal-api-contract.yaml`, AsyncAPI) **sem alteração** no diff.

## Conformidade com contratos

| Ponto | Contrato | Implementação | Situação |
|---|---|---|---|
| Rota pública | `POST /api/v1/staff-password-reset-requests`, `requestStaffPasswordReset`, `security: []` | `StaffPasswordRecoveryEndpoints` (BFF), sem sessão | OK |
| Rota interna | `POST /internal/v1/staff-password-reset-requests`, `requestStaffPasswordResetInternal`, asserção `bff-admin` | Endpoint Identity verifica Bearer + escopo `staff-passwords:reset`; tenant da asserção | OK |
| `Email` | `format: email`, `maxLength: 254` | BFF, Identity endpoint, validator e zod: `EmailAddress` + 254 | OK |
| `Idempotency-Key` | obrigatório, 1..128 | BFF e Identity: não vazio, ≤ 128; SPA envia `crypto.randomUUID()` | OK |
| 202 sem corpo | idêntico para as três situações | Use case sempre devolve 202; BFF devolve 202 sem corpo (teste E2E verifica corpo vazio) | OK |
| 422 | Identity `IDEMPOTENCY_CONFLICT` → público `IDEMPOTENCY_KEY_REUSED` | `StaffPasswordRecoveryException` (422) via `GlobalExceptionHandler`; BFF traduz (teste E2E) | OK |
| 400 | `INVALID_REQUEST` | JSON inválido/campos inválidos → `INVALID_REQUEST` nas duas camadas | OK |
| Pedido de envio | `recuperacao-de-senha`, link do backoffice, só conta interna | `StaffPasswordRecoveryMessageWriter` já existente (2.0); teste confirma finalidade, modelo, link `/admin/redefinir-senha?token=` e hash do token | OK |

O ponto do contexto sobre `AtoPraticadoPayload` não se aplica a esta fatia: `requestStaffPasswordReset` não
produz ato de auditoria (tabela "Relação entre modalidades" de `contracts.md`); o diff não publica ato.

## Comportamento (RN-03, RN-04, RN-07, RF-11)

- **Neutralidade:** o teste de integração executa conta interna, conta de aluno e e-mail inexistente e
  compara as três saídas; exatamente um token e uma mensagem de outbox, para a conta interna.
  `FindEligibleStaffByEmailAsync` filtra `AccountType.InternalActor` e `DeactivatedOn == null` — conta
  desativada não recebe e-mail.
- **Idempotência:** replay com mesmo e-mail normalizado devolve 202 sem novo token/mensagem; e-mail diferente
  com mesma chave → `IDEMPOTENCY_CONFLICT`. Tratamento de corrida espelha o caso de uso do aluno.
- **RN-07:** a redefinição entregue em 2.0 (`ResetStaffPassword.cs:120-126`) revoga as sessões internas não
  revogadas; o link gerado aponta para essa tela.
- **Recuperação do aluno** continua restrita a `AccountType.Student` (store inalterado nessa parte).
- **SPA:** "Esqueci a senha" na entrada leva a `/recuperar-senha`; tela mostra a mesma mensagem neutra após 202
  e erro genérico em falha.
- **DI:** `RequestStaffPasswordReset` registrado pelo scan de `IUseCase<,>` (Scrutor); validator registrado
  explicitamente; build e suítes completas passam.

## Bloqueantes

Nenhum.

## Recomendações (não bloqueiam)

1. **Endpoint interno do Identity sem teste HTTP.** Os testes do Identity chamam o caso de uso direto; nenhum
   teste exercita `/internal/v1/staff-password-reset-requests` (401 sem asserção/escopo errado, 400, 422 via
   handler global). Mesmo padrão das tasks anteriores (não há teste HTTP de nenhum endpoint `internal/v1/staff-*`
   em `Identity.EndToEndTests`). O smoke da full deve cobrir o caminho real BFF → Identity.
   `src/identity/src/CodeForCoders.Identity.Api/Endpoints/StaffPasswordRecoveryEndpoints.cs:29`.
2. **Neutralidade de tempo.** O caminho da conta interna gera token e grava outbox; aluno e inexistente não.
   Diferença pequena (mesma transação, sem envio síncrono) e idêntica ao precedente do aluno; avaliar
   equalização se RN-04 exigir resistência a enumeração por latência.
   `.../RequestStaffPasswordReset/RequestStaffPasswordReset.cs:47-62`.
3. **`additionalProperties: false`.** O `EmailRequest`/`EmailInput` proíbem campos extras, mas BFF e Identity
   desserializam ignorando propriedades desconhecidas. Mesmo comportamento dos endpoints anteriores; alinhar
   de forma transversal, se desejado.
4. **Respostas 502/504 no BFF** não constam do contrato público desta operação. Segue o precedente de
   `resetStaffPassword` (`StaffPasswordResetEndpoints.cs:20-21`); registrar no contrato numa evolução futura.

## Integridade da revisão

- HEAD antes e depois: `868d173bda9ebfc5c03e479dae5bbff579b5916d`; `git status --porcelain` com as mesmas 25
  entradas antes e depois. Nenhum arquivo de código, task ou estado foi editado pelo validator.
