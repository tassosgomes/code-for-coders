# Revisão da task 2.0 — confirmar conta e reenviar link

Run: run.brWRItTC

- Modo: focused · Tentativa: 2/3 · HEAD: `d17a0e572045b625d4df8e7ae6e52829d9431886`.
- Escopo: diff da task desde o checkpoint de 1.0, arquivos novos e task 2.0; confrontei `techspec.md#v-02`, OpenAPI público/interno, AsyncAPI de CAP-026 e skills .NET/React.
- HEAD e estado da implementação permaneceram estáveis durante a revisão.

## Gate

Comando do frontmatter executado via `rtk proxy` para preservar os argumentos nativos. O wrapper `rtk dotnet test` inicial interpretou a sintaxe como seu próprio runner e saiu 1; a execução nativa declarada terminou com exit 0.

- Identity: 4 passaram; BFF: 3 passaram; SPA: 2 selecionados passaram (4 filtrados).
- Total: 9 testes selecionados; `gate_expect` de pelo menos 7 atendido. Gate: **passed**.

## Revisão semântica

**Sem bloqueantes.** Identity valida tenant, finalidade, validade e uso do token; consumo, confirmação, evento e resultado idempotente são gravados no mesmo `CommitAsync`. Concorrência é protegida por tokens EF e há teste de duas confirmações. Reenvio retorna resposta neutra, seleciona aluno ativo ainda não confirmado e grava token, pedido novo e idempotência junto; os testes conferem payload, exchange e ausência de nova Conta/Credencial.

A SPA substitui a URL sem token antes de chamar o BFF. BFF propaga `Idempotency-Key` e escopos corretos; retries de métodos inseguros estão desabilitados. Identity e Notificação usam `ACCOUNT_CONFIRMATION_VALIDITY_HOURS` no Compose.

## Recomendações

1. **Smoke de entrega pendente:** não verifiquei o caminho de reenvio até a captura em smtp4dev. Não há container smtp4dev ativo; o stack encontrado está em execução há 17 horas e não foi reiniciado. `docker compose config` também não resolveu sem as chaves locais obrigatórias. Execute esse smoke em stack isolado antes da validação full.
2. **Erro de confirmação na SPA:** `student-confirmation-screen.tsx:58` trata qualquer falha do BFF/Identity como link inválido; a falha do próprio reenvio também retorna a esse estado em `:74-75`. Diferencie erro transitório de token inválido para não induzir reenvio após indisponibilidade.

## Veredito

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 2 recomendações. Nenhum código, task, estado ou commit foi alterado pelo validator.

## Revalidação — tentativa 1/3

Run: run.fV8uCXFv

- HEAD revisado: `9b896d667a4e58dc75e96f961a8696b0ce8497ee`; permaneceu igual durante a revisão. O diff da task contém a extração do callback de reenvio para `submitConfirmationRequest` e sua chamada pelo handler `onSubmit`; `git diff --check` passou.
- Gate declarado executado via `rtk proxy`: exit 0. Identity 4/4, BFF 3/3 e SPA filtrada 2/2; 9 testes selecionados, acima dos 7 exigidos.
- Evidência adicional solicitada: `npm --prefix src/student-spa run test` — exit 0, 7 arquivos e 14/14 testes; `npx eslint src/features/student-confirmation/components/student-confirmation-screen.tsx` — exit 0.
- B2 da task 2.0 (`react-hooks/refs`) resolvido: leitura de `requestAttemptRef.current` ocorre no callback chamado pelo handler de evento. A regra permanece habilitada e o lint do arquivo está limpo. A verificação do writer confirma o uso de `StudentAccount:ConfirmationBaseUrl`; appsettings/Compose apontam para `/student/confirm-account`, alinhado ao `basename` da SPA e à rota `/confirm-account`.
- Sem bloqueantes novos. Permanecem as duas recomendações não bloqueantes do parecer anterior: smoke do reenvio até smtp4dev não executado e tratamento de falha da confirmação ainda pode apresentar erro transitório como link inválido.

**VALIDAÇÃO APROVADA** — 0 bloqueantes, 2 recomendações herdadas. Esta aprovação cobre a task 2.0; findings de outras tasks permanecem fora deste escopo.
