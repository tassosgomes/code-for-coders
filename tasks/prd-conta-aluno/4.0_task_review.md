# Revalidação — task 4.0

Run: run.iZ0mdACo
Modo: revalidation · Tentativa: 2/3
Resultado: **VALIDAÇÃO APROVADA** · Gate: **passed**

## Gate declarado

- Reexecutei o gate exato do frontmatter. A primeira execução saiu com 2 por falha de inicialização do Testcontainers (`Could not find resource 'DockerContainer'`); a repetição completa terminou com exit 0.
- A repetição passou: Identity 6/6, BFF 5/5 e SPA 3/3 (14 testes, acima dos 7 mínimos). Evidências: `.tsg-flow/delegate-logs/run.iZ0mdACo/gate.log` e `gate-retry.log`.

## Revalidação do bloqueio anterior

- `ServiceAssertionTokenFactory` agora está registrado no DI em `ServiceConfigurationExtensions.cs:34`, com teste de resolução em `StudentPasswordRecoveryTests.cs:46`. `bff-student` está saudável no Compose.
- Smoke público pelo BFF: cadastro e reenvio retornaram 202; smtp4dev capturou 2 confirmações e 1 recuperação; confirmação e redefinição retornaram 204. Login com a senha antiga retornou 401 e com a nova 200. Evidência resumida em `.tsg-flow/delegate-logs/run.iZ0mdACo/smoke.log`; tokens não foram registrados.
- A correção da cifra do payload endereçado no outbox, revisada na tentativa anterior, não apresentou regressão observável.

## Bloqueantes e recomendações

- Nenhum bloqueante ou recomendação não bloqueante.

## Escopo e integridade

- Revisei a correção do DI e o smoke no Compose, além do gate e do relatório anterior. HEAD permaneceu `208738602bbc62feecd68e3e4ae356e8fd4b6f1a`; não alterei implementação nem commits durante a validação.

## Tentativa atual — revalidation 1/3
Run: run.1nsMhnFs
Resultado: **VALIDAÇÃO APROVADA** · Gate: **passed**

### Evidências
- Gate declarado, exit 0: Identity 6/6, BFF 5/5 e SPA focada 3/3; total 14 (mínimo 7). Log: `.tsg-flow/delegate-logs/run.1nsMhnFs/gate.log`. O wrapper `rtk dotnet` rejeitou a sintaxe antes de iniciar; executei o mesmo gate sem alterações via `rtk proxy`.
- Reabertura B2: o componente de recuperação não lê refs durante render; `npm --prefix src/student-spa run lint` passou e a suíte SPA completa passou 14/14 (92,34% linhas). B1 do dashboard também não se reproduziu.
- URL: Compose, configuração Identity e documentação usam `/student/redefinir-senha`. O container Identity está com essa URL; GET em `/student/redefinir-senha?token=validator-smoke` retornou HTTP 200 (`text/html`).

### Bloqueantes e recomendações
- Nenhum bloqueante ou recomendação não bloqueante.

### Escopo e integridade
- Revisei as correções do componente, configuração Compose/Identity, documentação, task reaberta e achados da revisão full. HEAD permaneceu `9dca8407c84cdad0a233c1980ad3a66245e6c7a6`; sem alterações em código, tasks, estado ou commits durante a validação.

## Tentativa atual — revalidation 1/3

Run: run.gaGPNXO3
Modo: revalidation · Tentativa: 1/3
Resultado: **VALIDAÇÃO APROVADA** · Gate: **passed**

### Gate e evidências

- Executei o gate exato do frontmatter; exit 0. Identity: 6/6, BFF: 5/5 e SPA focada: 3/3; total 14, acima do mínimo de 7. Log: `.tsg-flow/delegate-logs/run.gaGPNXO3/gate.log`.
- `npm --prefix src/student-spa run lint` passou; a suíte SPA completa passou com 16/16 testes em 8 arquivos, incluindo o teste novo do sanitizador. Cobertura: 92,71% de linhas. Evidência: `.tsg-flow/delegate-logs/run.gaGPNXO3/spa-lint-and-tests.log`.
- O build da imagem executou `npm run build` com sucesso. Evidência: `.tsg-flow/delegate-logs/run.gaGPNXO3/spa-image-build.log`.

### Revalidação do bloqueio B1 — token em logs/telemetria

- `nginx.conf.template` agora registra método, caminho sem query, status e user-agent, sem `$request`, `$request_uri` ou referer. Construí e iniciei a imagem da SPA em um container isolado, usando alias local para o upstream não utilizado. GET em `/student/redefinir-senha?token=probe` retornou HTTP 200 e o access log registrou `GET /student/redefinir-senha HTTP/1.1`, sem `token=`. Evidências: `.tsg-flow/delegate-logs/run.gaGPNXO3/spa-nginx-startup.log` e `spa-nginx-access.log`.
- `telemetry.ts` instala o processador de redação antes do exportador. O processador remove valores de `token` dos atributos de spans, eventos e links antes de o exportador recebê-los. O teste cobre `url.full`, `url.query`, `http.url`, eventos, links e listas de strings, e confirma que os segredos não aparecem nos spans exportados.

### Bloqueantes e recomendações

- Nenhum bloqueante ou recomendação não bloqueante.

### Escopo e integridade

- Revisei as correções correspondentes à reabertura B1. HEAD permaneceu `d8badd8cc0ee6774ca15799b6c94bf5ca2a204ed` e a árvore `77f8af8fbb98b9a744596759d9c64437830ce16a`; o status de implementação permaneceu igual ao baseline. Não alterei código, task state ou commits durante a validação.
