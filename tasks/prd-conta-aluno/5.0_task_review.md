# Revisão focada — Task 5.0

Run: run.wHK7dZBN

## Veredito da revisão anterior

VALIDAÇÃO REPROVADA — gate `passed` (exit code 0), mas há um requisito de verificação da TechSpec sem evidência.

## Gate

- Identity: `dotnet test src/identity/tests/CodeForCoders.Identity.IntegrationTests/CodeForCoders.Identity.IntegrationTests.csproj -- --filter-class CodeForCoders.Identity.IntegrationTests.StudentPasswordChangeTests --minimum-expected-tests 4` — 4 passaram.
- BFF: `dotnet test src/bff-student/tests/CodeForCoders.BffStudent.EndToEndTests/CodeForCoders.BffStudent.EndToEndTests.csproj -- --filter-class CodeForCoders.BffStudent.EndToEndTests.StudentPasswordChangeTests --minimum-expected-tests 2` — 3 passaram.
- SPA: `npm --prefix src/student-spa run test -- -t StudentPasswordChange` — 2 passaram.
- Total: 9 testes passaram; `gate_expect` atendido.

## Escopo revisado

Task 5.0 e TechSpec V-05; contratos público, interno e AsyncAPI; implementação e testes de Identity, BFF e SPA; middleware de sessão/CSRF; persistência de sessão, tokens, credencial e outbox; ADR-0003. Diff desde o checkpoint `991e3e430c93021b0086d4f6b6b2b2d0e49edfdb`.

## Bloqueante

- A TechSpec exige teste de contrato da interface interna em cada checkpoint (`techspec.md:214`), mas `StudentPasswordChangeTests` de Identity chama diretamente o caso de uso. O teste end-to-end do BFF injeta `StudentPasswordChangeIdentityClientStub` (`BffStudentApiFactory.cs:65-67`), então nenhum teste exercita `POST /internal/v1/password-changes`, sua asserção de serviço/escopo, binding da requisição e códigos HTTP (`StudentPasswordChangeEndpoints.cs:17-20`). Adicionar evidência de contrato para essa operação antes de aprovar.

## Recomendações

- `StudentIdentityOptions.Scope` ainda não lista `student-password-changes:execute` (`StudentIdentityOptions.cs:13`), embora `appsettings.json` e `docker-compose.yml` já incluam o escopo. Alinhar o valor padrão ou validar a presença desse escopo no startup para evitar falha de `ServiceAssertionTokenFactory.Create` em hosts que usem o padrão.
- Os contratos público e interno declaram `additionalProperties: false`, mas as opções de desserialização dos dois endpoints usam o comportamento padrão, que ignora propriedades desconhecidas. Rejeitar propriedades não mapeadas ou ajustar explicitamente o contrato.

## Integridade da revisão

`HEAD` permaneceu em `991e3e430c93021b0086d4f6b6b2b2d0e49edfdb`; o status do código permaneceu igual ao início da revisão. `git diff --check` não apontou problemas.

## Revalidação — 2026-09-23

Run: run.khN344WD

**Veredito atual: VALIDAÇÃO APROVADA** — 0 bloqueantes, 2 recomendações; gate passed.

### Evidências

- A tentativa via wrapper `rtk dotnet test` não chegou ao runner porque o wrapper exigiu `--project`; a mesma sequência do frontmatter foi então executada com `rtk proxy dotnet test` (e `rtk proxy npm`), preservando os argumentos declarados. Identity Integration: 4 passaram; BFF E2E: 3 passaram; SPA: 2 passaram. Exit code 0; gate_expect de pelo menos 7 atendido.
- O bloqueio anterior sobre ausência de teste da interface interna foi resolvido. `StudentPasswordChangeEndpointTests` exercita POST `/internal/v1/password-changes`, escopo de serviço, binding, respostas de erro, troca, replay/idempotência; executei o filtro da classe e 4 testes passaram (exit 0). Contrato corresponde a `/password-changes` em `internal-api-contract.yaml:183`.
- Nenhum novo bloqueante ou regressão foi identificado na revalidação.

### Bloqueantes

- Nenhum. O único bloqueante da revisão anterior foi fechado pela evidência end-to-end da rota interna descrita acima.

### Recomendações ainda aplicáveis

- `StudentIdentityOptions.Scope` ainda omite `student-password-changes:execute` no valor padrão (`src/bff-student/src/CodeForCoders.BffStudent.Api/Security/StudentIdentityOptions.cs:13`), embora a configuração local o inclua.
- Os dois endpoints de troca desserializam com opções padrão (`src/identity/src/CodeForCoders.Identity.Api/Endpoints/StudentPasswordChangeEndpoints.cs:13` e `src/bff-student/src/CodeForCoders.BffStudent.Api/Endpoints/StudentPasswordChangeEndpoints.cs:14`), que ignoram campos não mapeados enquanto o contrato define `additionalProperties: false`.

### Integridade da revalidação

HEAD permaneceu em `991e3e430c93021b0086d4f6b6b2b2d0e49edfdb`; o status da árvore permaneceu igual ao início. `git diff --check` passou.
